



const GAME_VERSION='0.1.15';
const CZ_BOOT=window.CZ_BOOT||{uiReady:false,attempts:0,lastError:null};window.CZ_BOOT=CZ_BOOT;const MENU_UI_SELECTOR='.menu-btn,.lan-item,#main-menu label,#mp-panel input';function getAudioRTSSafe(){const audio=window.AudioRTS;return audio&&typeof audio.ui==='function'?audio:null;}function playMenuUi(kind='click'){try{const audio=getAudioRTSSafe();if(!audio)return;if(kind==='click')audio.touch();audio.ui(kind);}catch(_){}}function installMenuUiAudio(){if(CZ_BOOT.menuAudioBound)return;CZ_BOOT.menuAudioBound=true;let lastHover=0;document.addEventListener('pointerover',e=>{const el=e.target&&e.target.closest?e.target.closest(MENU_UI_SELECTOR):null;if(!el)return;const n=performance.now();if(n-lastHover<70)return;lastHover=n;playMenuUi('select');},{passive:true});document.addEventListener('pointerdown',e=>{const el=e.target&&e.target.closest?e.target.closest(MENU_UI_SELECTOR):null;if(!el)return;playMenuUi('click');},{passive:true});document.addEventListener('focusin',e=>{const el=e.target&&e.target.closest?e.target.closest('#mp-panel input,.menu-btn'):null;if(el)playMenuUi('select');});}function syncGameVersionUi(){let versionEl=document.getElementById('menu-version');if(!versionEl){const subtitle=document.querySelector('#main-menu .menu-subtitle');if(subtitle){versionEl=document.createElement('span');versionEl.id='menu-version';versionEl.style.fontSize='10px';versionEl.style.color='#4a7a8a';versionEl.style.display='block';versionEl.style.marginTop='2px';subtitle.appendChild(versionEl);}}if(versionEl)versionEl.textContent=`v${GAME_VERSION}`;}function safeBootStep(label,fn){try{fn();return true;}catch(err){CZ_BOOT.lastError=`${label}: ${err&&err.message?err.message:err}`;console.warn(`[CZ-BOOT] ${label} failed:`,err);return false;}}function ensureUiBootstrap(force=false){if(CZ_BOOT.uiReady&&!force)return true;CZ_BOOT.attempts=(CZ_BOOT.attempts||0)+1;const okVersion=safeBootStep('menu-version',syncGameVersionUi),okMenu=safeBootStep('menu-audio',installMenuUiAudio),okSettings=safeBootStep('settings-ui',initSettingsUi),okAuto=safeBootStep('auto-ai',initAutoAiToggle);CZ_BOOT.uiReady=!!(okVersion&&okMenu&&okSettings&&okAuto);if(!CZ_BOOT.uiReady&&CZ_BOOT.attempts<120)requestAnimationFrame(()=>ensureUiBootstrap());return CZ_BOOT.uiReady;}function scheduleUiBootstrap(){if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>ensureUiBootstrap(),{once:true});else requestAnimationFrame(()=>ensureUiBootstrap());}scheduleUiBootstrap();
const canvas=document.getElementById('canvas');const ctx=canvas.getContext('2d');function resize(){const container=document.getElementById('canvas-container');canvas.width=container.clientWidth;canvas.height=container.clientHeight;if(window.threeFP&&threeFP.renderer&&threeFP.camera){threeFP.renderer.setSize(container.clientWidth,container.clientHeight,false);threeFP.camera.aspect=container.clientWidth/Math.max(1,container.clientHeight);threeFP.camera.updateProjectionMatrix();try{threeFPHudLayout();}catch(_){}}try{applyMobileCanvasTransform();}catch(_){}}
resize();window.addEventListener('resize',()=>{resize();try{updateMobileLayoutMetrics();}catch(_){}try{updateMobileFullscreenButton();}catch(_){}});setTimeout(()=>{try{installMobileBrowserMode();}catch(_){}} ,0);const TILE=32;const MAP_W=280,MAP_H=200;const DEFAULT_UNIT_MODS=Object.freeze({unit:{cost:25,hp:60,range:75,damage:4},worker:{cost:15,hp:20,range:0,damage:0},leader:{cost:75,hp:120,range:0,damage:0},tank:{cost:200,hp:280,range:150,damage:8},drone:{cost:20,hp:1,range:55,damage:1},helicopter:{cost:350,hp:95,range:260,damage:5},ship:{cost:80,hp:200,range:300,damage:10},bike:{cost:100,hp:35,range:80,damage:3},patriot:{cost:1300,hp:220,range:70,damage:9999},supply:{cost:260,hp:170,range:0,damage:0},launcher:{cost:520,hp:140,range:620,damage:34}});function clampUnitModNumber(value,fallback){const n=Number(value);return Number.isFinite(n)?n:fallback;}
function buildUnitMods(defaults,externalMods){const result={};const src=(externalMods&&typeof externalMods==='object')?externalMods:{};for(const[type,cfg]of Object.entries(defaults)){const ext=(src[type]&&typeof src[type]==='object')?src[type]:{};result[type]={cost:clampUnitModNumber(ext.cost,cfg.cost),hp:clampUnitModNumber(ext.hp,cfg.hp),range:clampUnitModNumber(ext.range,cfg.range),damage:clampUnitModNumber(ext.damage,cfg.damage)};}
return Object.freeze(result);}
const UNIT_MODS=buildUnitMods(DEFAULT_UNIT_MODS,window.UNIT_MODS);function getUnitMod(type){return UNIT_MODS[type]||DEFAULT_UNIT_MODS[type]||{cost:0,hp:0,range:0,damage:0};}
const SETTINGS_STORAGE_KEY=window.SETTINGS_STORAGE_KEY||'command-zero-settings-v57';window.SETTINGS_STORAGE_KEY=SETTINGS_STORAGE_KEY;const gameSettings=window.gameSettings||(window.gameSettings={texturedTerrain:true});
function loadGameSettings(){try{const raw=localStorage.getItem(SETTINGS_STORAGE_KEY);if(!raw)return;const parsed=JSON.parse(raw);if(parsed&&typeof parsed==='object'&&typeof parsed.texturedTerrain==='boolean')gameSettings.texturedTerrain=parsed.texturedTerrain;}catch(err){console.warn('Settings load failed:',err);}}
function saveGameSettings(){try{localStorage.setItem(SETTINGS_STORAGE_KEY,JSON.stringify(gameSettings));}catch(err){console.warn('Settings save failed:',err);}}
function updateTextureModeUi(){const toggle=document.getElementById('texture-toggle-menu');if(toggle)toggle.checked=!!gameSettings.texturedTerrain;}
function setTexturedTerrain(enabled){const next=!!enabled;if(gameSettings.texturedTerrain===next){updateTextureModeUi();return;}gameSettings.texturedTerrain=next;saveGameSettings();updateTextureModeUi();markMinimapDirty();}
function initSettingsUi(){loadGameSettings();updateTextureModeUi();const toggle=document.getElementById('texture-toggle-menu');if(toggle&&!toggle.dataset.bound){toggle.dataset.bound='1';toggle.addEventListener('change',e=>setTexturedTerrain(e.target.checked));}}
function formatUnitStat(value){return Number.isFinite(value)?String(value):'0';}
const BASE_HP=750,BASE_COST=950;const UNIT_COST=getUnitMod('unit').cost;const WORKER_COST=getUnitMod('worker').cost;const UNIT_HP=getUnitMod('unit').hp;const WORKER_HP=getUnitMod('worker').hp;const UNIT_SPEED=1.755,UNIT_RANGE=getUnitMod('unit').range,UNIT_AA_RANGE=165,UNIT_DAMAGE=getUnitMod('unit').damage,UNIT_FIRE_RATE=70;const UNIT_RADIUS=10;const MINE_COST=40,MINE_COOLDOWN=0,MINE_PLACE_TICKS=30*60,MINE_KILL_RADIUS=TILE*1.25,MINE_DAMAGE=120,MINE_ARM_TICKS=120;const TANK_COST=getUnitMod('tank').cost,TANK_HP=getUnitMod('tank').hp,TANK_SPEED=1.85,TANK_RANGE=getUnitMod('tank').range;const TANK_DAMAGE=getUnitMod('tank').damage,TANK_FIRE_RATE=100,TANK_SPLASH=TILE*0.5;const TANK_INACCURACY=18;const TRAIN_TIME_TANK=540;const AI_TRAIN_TIME_TANK=480;const TOWER_COST=100,TOWER_HP=105,TOWER_RANGE_NEAR=84,TOWER_RANGE_FAR=157.5,TOWER_AA_RANGE_NEAR=173.25,TOWER_AA_RANGE_FAR=299.25;const TOWER_DAMAGE_NEAR=5,TOWER_FIRE_RATE_NEAR=60;const TOWER_FIRE_RATE_FAR=110;const TOWER_FAR_CHANCE=0.35;const SUPER_TOWER_COST=TOWER_COST*3,SUPER_TOWER_HP=TOWER_HP,SUPER_TOWER_RANGE_MIN=TOWER_RANGE_FAR,SUPER_TOWER_RANGE_MAX=TOWER_RANGE_FAR*3,SUPER_TOWER_AA_RANGE_MIN=TOWER_AA_RANGE_FAR,SUPER_TOWER_AA_RANGE_MAX=TOWER_AA_RANGE_FAR*3,SUPER_TOWER_FIRE_RATE=TOWER_FIRE_RATE_FAR;const TOWER_MAX_DIST=20;const WALL_MAX_DIST=24;const LEADER_COST=getUnitMod('leader').cost,LEADER_HP=getUnitMod('leader').hp,LEADER_SPEED=1.755,LEADER_RALLY_RADIUS=15*32;const BASE_MIN_DIST_FROM_ENEMY=40;const BUILD_TIME_BASE=9000;const BUILD_TIME_TOWER=3600;const WORKER_BUILD_BONUS=0.15;const WORKER_SPEED=2.0,WORKER_CARRY=25,WORKER_MINE_TIME=50;const WORKER_DECONSTRUCT_DAMAGE=3,WORKER_DECONSTRUCT_RATE=40,WORKER_DECONSTRUCT_RANGE=28,WORKER_DECONSTRUCT_REFUND=0.6;const RESOURCE_HP=6;const AGGRO_RANGE=110;const BASE_INCOME=1;const INCOME_INTERVAL=120;const TRAIN_TIME_UNIT=360;const TRAIN_TIME_WORKER=270;const AI_TRAIN_TIME_UNIT=360;const AI_TRAIN_TIME_WORKER=270;const EDGE_SCROLL_SPEED=8;const HOUSE_COST=60,HOUSE_HP=80,HOUSE_CAP=6;const BASE_POP_CAP=10;const BUILD_TIME_HOUSE=1200;const APARTMENT_COST=200,APARTMENT_HP=140,APARTMENT_CAP=20;const BUILD_TIME_APARTMENT=1200;const TRAINING_COST=150,TRAINING_HP=160;const BUILD_TIME_TRAINING=1200;const VILLAGER_HP=18,VILLAGER_SPEED=2.35;const VILLAGER_SPAWN_INTERVAL=720;const VILLAGER_LIFE=7200;const VILLAGER_FLEE_RANGE=150;const VILLAGER_FLEE_COOLDOWN=120;const VILLAGER_WANDER_INTERVAL=240;const VILLAGER_WANDER_RADIUS=260;const TRAINING_CAP=5;const TRAINING_CONVERT_TIME=TRAIN_TIME_UNIT;const HOUSE_MAX_DIST=7;const DRONE_COST=getUnitMod('drone').cost,DRONE_HP=getUnitMod('drone').hp,DRONE_SPEED=4.2;const DRONE_DAMAGE=getUnitMod('drone').damage,DRONE_FIRE_RATE=90,DRONE_RANGE=getUnitMod('drone').range;const TRAIN_TIME_DRONE=360;const AI_TRAIN_TIME_DRONE=240;const HELICOPTER_COST=getUnitMod('helicopter').cost,HELICOPTER_HP=getUnitMod('helicopter').hp,HELICOPTER_SPEED=5.10;const HELICOPTER_DAMAGE=getUnitMod('helicopter').damage,HELICOPTER_FIRE_RATE=48,HELICOPTER_RANGE=getUnitMod('helicopter').range;const HELICOPTER_POP_COST=3;const HELICOPTER_BOMB_COOLDOWN=1980;const HELICOPTER_BOMB_SPLASH=TILE*1.05;const HELICOPTER_BOMB_DAMAGE=26;const HELICOPTER_ROCKET_SPLASH=TILE*0.42;const HELICOPTER_ROCKET_SPLASH_DAMAGE=HELICOPTER_DAMAGE*0.35;const TRAIN_TIME_HELICOPTER=720;const HELICOPTER_ALTITUDE=128;const HELICOPTER_TOWER_MULT=3.4;const HELICOPTER_SOLDIER_MULT=0.38;const HELICOPTER_DRONE_MULT=0.9;const TRAIN_TIME_LEADER=360;const AI_TRAIN_TIME_LEADER=330;const AI_LEADER_MAX=2;const PLAYER_LEADER_MAX=3;const AI_DRONE_MAX=18;const DRONE_MAX_PLAYER=10;const WALL_COST=12,WALL_HP=90;const BUILD_TIME_WALL=900;const RAIL_COST=2,RAIL_HP=20,BUILD_TIME_RAIL=480;const CRANE_COST=20,CRANE_HP=10,BUILD_TIME_CRANE=600,CRANE_MOVE_SPEED=1.35,CRANE_BUILD_RADIUS=30*TILE,CRANE_BUILD_MULT=4;const TRAIN_COST=90,TRAIN_HP=220,BUILD_TIME_TRAIN=1500,TRAIN_MOVE_SPEED=2.48,TRAIN_CARGO_CAPACITY=200,TRAIN_DWELL_TICKS=18,TRAIN_ROUTE_REPATH_TICKS=60;const TANK_HP_NEW=getUnitMod('tank').hp;const HARBOR_COST=80,HARBOR_HP=180,BUILD_TIME_HARBOR=2700;const SILO_COST=3000,SILO_HP=4200,BUILD_TIME_SILO=3600,SILO_MAX_DIST=10;const RADAR_COST=1500,RADAR_HP=200,BUILD_TIME_RADAR=900,RADAR_MAX_DIST=15;const RADAR_RANGE_TILES=100;const NUKE_COUNTDOWN=100*60,NUKE_CORE_RADIUS=15*TILE,NUKE_OUTER_RADIUS=23*TILE,NUKE_OUTER_DAMAGE=900;const SHIP_COST=getUnitMod('ship').cost,SHIP_HP=getUnitMod('ship').hp,SHIP_SPEED=1.8;const SHIP_RANGE=getUnitMod('ship').range;const SHIP_DAMAGE=getUnitMod('ship').damage,SHIP_FIRE_RATE=120,SHIP_SPLASH=TILE*0.8;const SHIP_INACCURACY=22;const TRAIN_TIME_SHIP=720;const AI_TRAIN_TIME_SHIP=750;const TANK_WATER_SPEED=TANK_SPEED*0.10;const BIKE_COST=getUnitMod('bike').cost,BIKE_HP=getUnitMod('bike').hp,BIKE_SPEED=4.39,BIKE_RANGE=getUnitMod('bike').range;const BIKE_DAMAGE=getUnitMod('bike').damage,BIKE_FIRE_RATE=40;const BIKE_TRAIN_TIME=360;const BIKE_WATER_SPEED=BIKE_SPEED*0.25;const PATRIOT_COST=getUnitMod('patriot').cost,PATRIOT_HP=210,PATRIOT_SPEED=1.52,PATRIOT_RANGE=getUnitMod('patriot').range,PATRIOT_MIN_HELI_RANGE=10,PATRIOT_HELI_DAMAGE=getUnitMod('patriot').damage,PATRIOT_HELI_COOLDOWN=30*60,PATRIOT_NUKE_COOLDOWN=60*60,PATRIOT_NUKE_INTERCEPT_CHANCE=0.65,PATRIOT_NUKE_PROTECT_RADIUS=TILE*12,PATRIOT_POP_COST=1,PATRIOT_TRAIN_TIME=9*60,PATRIOT_RADIUS=12,PATRIOT_WATER_SPEED=PATRIOT_SPEED*0.10;const SUPPLY_COST=getUnitMod('supply').cost,SUPPLY_HP=getUnitMod('supply').hp,SUPPLY_SPEED=1.62,SUPPLY_POP_COST=1,SUPPLY_TRAIN_TIME=7*60,SUPPLY_RADIUS=12,SUPPLY_AURA_RADIUS=20*TILE,SUPPLY_WATER_SPEED=SUPPLY_SPEED*0.10;const LAUNCHER_COST=getUnitMod('launcher').cost,LAUNCHER_HP=140,LAUNCHER_SPEED=1.08,LAUNCHER_RANGE=getUnitMod('launcher').range,LAUNCHER_MIN_RANGE=120,LAUNCHER_DAMAGE=getUnitMod('launcher').damage,LAUNCHER_FIRE_RATE=360,LAUNCHER_SPLASH=TILE*1.65,LAUNCHER_INACCURACY=24,LAUNCHER_POP_COST=2,LAUNCHER_TRAIN_TIME=12*60,AI_TRAIN_TIME_LAUNCHER=11*60,LAUNCHER_RADIUS=12,LAUNCHER_WATER_SPEED=LAUNCHER_SPEED*0.10;const DAY_DURATION_TICKS=5*60*60;const NIGHT_DURATION_TICKS=80*60;const NIGHT_TEST_DURATION_TICKS=60*60;const NIGHT_RANGE_SCALE=0.74;const NIGHT_MINIMAP_BASE_VISION=TILE*8.5;const NIGHT_MINIMAP_UNIT_VISION=TILE*5.5;const NIGHT_2D_OVERLAY_ALPHA=0.28;const TEAM_COLORS={player:{main:'#00ff88',body:'#071c10',worker:'#00ff88',workerBody:'#071c10',tank:'#4cc6ff',tankBody:'#0b2230',minimapTank:'#4cc6ff'},ai:{main:'#ff3344',body:'#1c0707',worker:'#ff3344',workerBody:'#1c0707',tank:'#ff9a3c',tankBody:'#34160b',minimapTank:'#ff9a3c'}};const SPECIAL_UNIT_TEAM_COLORS={launcher:{player:{main:'#34c9ff',dark:'#0c2431',bright:'#9be7ff',rgb:'52,201,255'},ai:{main:'#ff4f8d',dark:'#32101c',bright:'#ff9bb9',rgb:'255,79,141'}},patriot:{player:{main:'#ffd24a',dark:'#35260b',bright:'#ffe891',rgb:'255,210,74'},ai:{main:'#ff7a3d',dark:'#37170c',bright:'#ffb185',rgb:'255,122,61'}},supply:{player:{main:'#83ff63',dark:'#153213',bright:'#c6ffb2',rgb:'131,255,99'},ai:{main:'#ad84ff',dark:'#21143a',bright:'#dbc6ff',rgb:'173,132,255'}}};function getSpecialUnitTheme(type,faction){const cfg=SPECIAL_UNIT_TEAM_COLORS[type];if(!cfg)return null;const side=getVisualFactionId(faction)==='player'?'player':'ai';return cfg[side]||null;}function getUnitAccentCss(type,faction,fallbackPlayer,fallbackAi){const theme=getSpecialUnitTheme(type,faction);return theme?theme.main:pickFactionColor(faction,fallbackPlayer,fallbackAi);}function getUnitAccentRgba(type,faction,fallbackPlayerRgb,fallbackAiRgb,alpha){const theme=getSpecialUnitTheme(type,faction);const rgb=theme?theme.rgb:(getVisualFactionId(faction)==='player'?fallbackPlayerRgb:fallbackAiRgb);return`rgba(${rgb},${alpha})`;}function isMirroredMultiplayerView(){return!!(typeof MP!=='undefined'&&MP&&MP.faction==='opponent');}
function getVisualFactionId(faction){if(!isMirroredMultiplayerView())return faction;if(faction==='player')return'ai';if(faction==='ai')return'player';return faction;}
function getFactionPalette(faction){return TEAM_COLORS[getVisualFactionId(faction)]||TEAM_COLORS.player;}
function getFactionMainColor(faction){return getFactionPalette(faction).main;}
function pickFactionColor(faction,playerCss,aiCss){return getVisualFactionId(faction)==='player'?playerCss:aiCss;}
function pickFactionRgba(faction,playerRgb,aiRgb,alpha){return`rgba(${getVisualFactionId(faction) === 'player' ? playerRgb : aiRgb},${alpha})`;}
function setSyncPoint(obj,x,y){if(!obj._syncTarget)obj._syncTarget={x,y};else{obj._syncTarget.x=x;obj._syncTarget.y=y;}}const RENDER_SMOOTH_TYPES=new Set(['unit','worker','villager','tank','drone','helicopter','ship','leader','bike','patriot','supply','launcher']);function isRenderSmoothedEntity(o){return!!(o&&RENDER_SMOOTH_TYPES.has(o.type));}function applyRenderSmoothing(o,tx,ty){if(typeof o._rx!=='number'||typeof o._ry!=='number'){o._rx=tx;o._ry=ty;o.x=tx;o.y=ty;return;}const dx=tx-o._rx,dy=ty-o._ry;const snapDist=TILE*1.6;if(dx*dx+dy*dy>snapDist*snapDist){o._rx=tx;o._ry=ty;o.x=tx;o.y=ty;return;}const moving=((o.vx||0)*(o.vx||0)+(o.vy||0)*(o.vy||0))>0.01||!!o.target||!!o.attackTarget||!!o._syncTarget;const ease=moving?0.42:0.62;o._rx+=dx*ease;o._ry+=dy*ease;if(Math.abs(tx-o._rx)<0.03)o._rx=tx;if(Math.abs(ty-o._ry)<0.03)o._ry=ty;o.x=o._rx;o.y=o._ry;}
function refreshTeamVisualState(){if(player&&player.color!==undefined)player.color=getFactionMainColor('player');if(ai&&ai.color!==undefined)ai.color=getFactionMainColor('ai');const own=document.querySelector('.player-stat');const opp=document.querySelector('.ai-stat');if(own){own.style.color=player.color;own.style.opacity='1';own.querySelectorAll('span').forEach(el=>{el.style.color=player.color;});}
if(opp){opp.style.color='#b8c0c8';opp.style.opacity='0.86';opp.querySelectorAll('span').forEach(el=>{el.style.color='#cfd5db';});}}
let camera={x:0,y:0};let selection=[];let mode='select';const MOBILE_BROWSER=((typeof navigator!=='undefined'&&/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent||''))||((typeof window!=='undefined'&&'ontouchstart'in window)&&typeof matchMedia==='function'&&matchMedia('(pointer:coarse)').matches&&Math.min(window.innerWidth||0,window.innerHeight||0)<=1200));const mobileView={enabled:!!MOBILE_BROWSER,zoom:1,minZoom:1,maxZoom:1,suppressMouseUntil:0,allowSyntheticMouse:false,commandArmed:false,lastTapTime:0,lastTapX:0,lastTapY:0,tapChain:0,lastTapTargetId:'',activeTouchId:null,touchStartX:0,touchStartY:0,startCameraX:0,startCameraY:0,touchMoved:false,touchStartTime:0,pinchActive:false,pinchStartDist:0,pinchStartZoom:1,pinchAnchorWorld:null,minimapDrag:false,touchHudTimer:0,fpHintTimer:0,footerHintTimer:0,fpMoveTouchId:null,fpLookTouchId:null,fpMoveX:0,fpMoveY:0,fpLookX:0,fpLookY:0};const MOBILE_COMBAT_SELECT_TYPES=new Set(['unit','tank','launcher','patriot','drone','helicopter','ship','bike','leader']);const MOBILE_NONCOMBAT_SELECT_TYPES=new Set(['worker','villager','supply']);let tick=0;let gameOver=false;let cheatPunishment={timer:null,deadline:0,active:false};const CTRL_ALT_9_CHEAT_WINDOW_TICKS=60*60;let dayNight={forceNightUntil:0,lastIsNight:false};const NIGHT_MOBILE_ATTACKERS=new Set(['unit','tank','launcher','drone','ship','helicopter','bike','leader']);const NIGHT_LIGHT_ENTITY_TYPES=new Set(['unit','worker','villager','tank','launcher','patriot','supply','drone','helicopter','ship','leader','base','tower','harbor','silo']);const nightVisionCache={tick:-1,factionId:'',baseRangeSq:NIGHT_MINIMAP_BASE_VISION*NIGHT_MINIMAP_BASE_VISION,unitRangeSq:NIGHT_MINIMAP_UNIT_VISION*NIGHT_MINIMAP_UNIT_VISION,baseCenters:[],unitCenters:[],visible:new WeakMap()};function dayNightPhaseInfo(t=tick){const forceUntil=Number.isFinite(dayNight.forceNightUntil)?dayNight.forceNightUntil:0;if(t<forceUntil)return{night:true,forced:true,remain:Math.max(0,forceUntil-t)};const cycle=DAY_DURATION_TICKS+NIGHT_DURATION_TICKS;const phase=((t%cycle)+cycle)%cycle;if(phase<DAY_DURATION_TICKS)return{night:false,forced:false,remain:DAY_DURATION_TICKS-phase};return{night:true,forced:false,remain:cycle-phase};}
function isNightTick(t=tick){return!!dayNightPhaseInfo(t).night;}
function isNightNow(){return isNightTick(tick);}
function getNightCombatRange(attacker,baseRange){if(!isNightNow()||!attacker||!NIGHT_MOBILE_ATTACKERS.has(attacker.type))return baseRange;return Math.max(24,baseRange*NIGHT_RANGE_SCALE);}
function nightRadarSourcesForFaction(faction='player'){const fac=getFaction(faction)||player;return[...(fac.bases||[]),...(fac.units||[]),...(fac.workers||[]),...(fac.tanks||[]),...(fac.launchers||[]),...(fac.patriots||[]),...(fac.supplies||[]),...(fac.drones||[]),...(fac.helicopters||[]),...(fac.ships||[]),...(fac.leaders||[]),...(fac.villagers||[])];}
function ensureNightVisionCache(faction='player'){const viewer=getFaction(faction)||player;const fid=String((viewer&&viewer.id)||faction||'player');if(nightVisionCache.tick===tick&&nightVisionCache.factionId===fid)return nightVisionCache;nightVisionCache.tick=tick;nightVisionCache.factionId=fid;nightVisionCache.visible=new WeakMap();nightVisionCache.baseCenters.length=0;nightVisionCache.unitCenters.length=0;for(const b of(viewer.bases||[])){if(!b||b.hp<=0)continue;const c=entityCenter(b);nightVisionCache.baseCenters.push({x:c.x,y:c.y});}
for(const s of nightRadarSourcesForFaction(faction)){if(!s||s.hp<=0||s.type==='base')continue;const c=entityCenter(s);nightVisionCache.unitCenters.push({x:c.x,y:c.y});}
return nightVisionCache;}
function isEntityVisibleOnNightRadar(entity,faction='player'){if(!entity||entity.hp<=0)return false;if(!isNightNow())return true;const cache=ensureNightVisionCache(faction);if(cache.visible.has(entity))return!!cache.visible.get(entity);const ec=entityCenter(entity);const ex=ec.x,ey=ec.y;let visible=false;for(const c of cache.baseCenters){const dx=ex-c.x,dy=ey-c.y;if(dx*dx+dy*dy<=cache.baseRangeSq){visible=true;break;}}
if(!visible){for(const c of cache.unitCenters){const dx=ex-c.x,dy=ey-c.y;if(dx*dx+dy*dy<=cache.unitRangeSq){visible=true;break;}}}
cache.visible.set(entity,visible);return visible;}
function isEntityVisibleAtNight(entity,faction='player'){if(!entity||entity.hp<=0)return false;if(!isNightNow())return true;const viewer=getFaction(faction)||player;if(entity.faction&&entity.faction===viewer.id)return true;return isEntityVisibleOnNightRadar(entity,faction);}
const STRUCTURE_DISCOVERY_RANGE=TILE*7.5;const STRUCTURE_DISCOVERY_RANGE_SQ=STRUCTURE_DISCOVERY_RANGE*STRUCTURE_DISCOVERY_RANGE;const DISCOVERABLE_STRUCTURE_TYPES=new Set(['base','tower','house','apartment','training','wall','harbor','silo','radar']);const DISCOVERABLE_SCOUT_TYPES=new Set(['unit','worker','tank','launcher','patriot','supply','drone','helicopter','ship','leader','villager']);const TERRITORY_DISCOVERY_RADIUS_TILES=7;const TERRITORY_DISCOVERY_RADIUS=TERRITORY_DISCOVERY_RADIUS_TILES*TILE;const WORKER_AUTO_RESOURCE_RADIUS_TILES=20;const WORKER_AUTO_RESOURCE_RADIUS=WORKER_AUTO_RESOURCE_RADIUS_TILES*TILE;function makeDiscoveryGrid(){return new Uint8Array(MAP_W*MAP_H);}
function tileDiscovered(grid,tx,ty){if(!grid)return false;if(tx<0||ty<0||tx>=MAP_W||ty>=MAP_H)return false;return grid[ty*MAP_W+tx]===1;}
function markDiscoveredCircle(grid,cx,cy,radiusTiles=TERRITORY_DISCOVERY_RADIUS_TILES){if(!grid)return;const tx=Math.floor(cx/TILE),ty=Math.floor(cy/TILE);const r=Math.max(1,radiusTiles|0),rr=r*r;const minX=Math.max(0,tx-r),maxX=Math.min(MAP_W-1,tx+r);const minY=Math.max(0,ty-r),maxY=Math.min(MAP_H-1,ty+r);for(let y=minY;y<=maxY;y++){for(let x=minX;x<=maxX;x++){const dx=x-tx,dy=y-ty;if(dx*dx+dy*dy<=rr)grid[y*MAP_W+x]=1;}}}
function updateTerritoryDiscovery(viewerFaction='player'){const viewer=getFaction(viewerFaction)||player;if(!viewer.discoveredTiles)viewer.discoveredTiles=makeDiscoveryGrid();const scouts=[...(viewer.units||[]),...(viewer.workers||[]),...(viewer.tanks||[]),...(viewer.launchers||[]),...(viewer.patriots||[]),...(viewer.supplies||[]),...(viewer.drones||[]),...(viewer.helicopters||[]),...(viewer.ships||[]),...(viewer.leaders||[]),...(viewer.villagers||[]),...(viewer.bikes||[])].filter(e=>e&&e.hp>0&&DISCOVERABLE_SCOUT_TYPES.has(e.type));for(const scout of scouts){const c=entityCenter(scout);markDiscoveredCircle(viewer.discoveredTiles,c.x,c.y);}
for(const b of(viewer.bases||[])){if(!b||b.hp<=0)continue;const c=entityCenter(b);markDiscoveredCircle(viewer.discoveredTiles,c.x,c.y,5);}
for(const r of(viewer.radars||[])){if(!r||r.hp<=0)continue;const c=entityCenter(r);markDiscoveredCircle(viewer.discoveredTiles,c.x,c.y,RADAR_RANGE_TILES);}}
function isTileDiscoveredForFaction(tx,ty,viewerFaction='player'){const viewer=getFaction(viewerFaction)||player;if(!viewer.discoveredTiles)viewer.discoveredTiles=makeDiscoveryGrid();return tileDiscovered(viewer.discoveredTiles,tx,ty);}
function isEntityInDiscoveredTerritory(entity,viewerFaction='player'){if(!entity)return false;const tx0=Math.max(0,Math.floor((entity.x||0)/TILE));const ty0=Math.max(0,Math.floor((entity.y||0)/TILE));const tw=Math.max(1,entity.tw||entity.w||Math.ceil((entity.w||TILE)/TILE));const th=Math.max(1,entity.th||entity.h||Math.ceil((entity.h||TILE)/TILE));for(let y=ty0;y<Math.min(MAP_H,ty0+th);y++){for(let x=tx0;x<Math.min(MAP_W,tx0+tw);x++){if(isTileDiscoveredForFaction(x,y,viewerFaction))return true;}}
if(entity.tx!==undefined&&entity.ty!==undefined)return isTileDiscoveredForFaction(entity.tx,entity.ty,viewerFaction);const c=entityCenter(entity);return isTileDiscoveredForFaction(Math.floor(c.x/TILE),Math.floor(c.y/TILE),viewerFaction);}
function autoDiscoverEnemyStructuresFromTerritory(viewerFaction='player'){const viewer=getFaction(viewerFaction)||player;const enemy=viewerFaction==='player'?ai:player;if(!viewer.discoveredEnemyStructures)viewer.discoveredEnemyStructures=new Set();const targets=[...(enemy.bases||[]),...(enemy.towers||[]),...(enemy.houses||[]),...((enemy.apartments)||[]),...((enemy.trainings)||[]),...(enemy.walls||[]),...(enemy.harbors||[]),...((enemy.silos)||[]),...((enemy.radars)||[]),...buildSites.filter(s=>s&&s.faction===enemy.id)];for(const target of targets){if(!target||target.hp<=0)continue;if(isEntityInDiscoveredTerritory(target,viewerFaction))viewer.discoveredEnemyStructures.add(structureDiscoveryKey(target));}}
function isEntityVisibleByDiscovery(entity,viewerFaction='player'){if(!entity||entity.hp<=0)return false;const viewer=getFaction(viewerFaction)||player;if(entity.faction===viewer.id)return true;return isEntityInDiscoveredTerritory(entity,viewerFaction);}
function structureDiscoveryKey(e){if(!e)return'';if(e.id!==undefined&&e.id!==null)return String(e.id);if(e.tx!==undefined&&e.ty!==undefined)return`${e.type}_${e.faction}_${e.tx}_${e.ty}`;return`${e.type}_${e.faction}_${Math.round(e.x||0)}_${Math.round(e.y||0)}`;}
function isEnemyStructureDiscovered(entity,viewerFaction='player'){if(!entity||entity.hp<=0)return false;const viewer=getFaction(viewerFaction)||player;if(entity.faction===viewer.id)return true;if(!DISCOVERABLE_STRUCTURE_TYPES.has(entity.type))return true;if(isEntityInDiscoveredTerritory(entity,viewerFaction)){if(!viewer.discoveredEnemyStructures)viewer.discoveredEnemyStructures=new Set();viewer.discoveredEnemyStructures.add(structureDiscoveryKey(entity));return true;}
const found=viewer.discoveredEnemyStructures;return!!(found&&found.has(structureDiscoveryKey(entity)));}
function updateEnemyStructureDiscovery(viewerFaction='player'){const viewer=getFaction(viewerFaction)||player;const enemy=viewerFaction==='player'?ai:player;if(!viewer.discoveredEnemyStructures)viewer.discoveredEnemyStructures=new Set();autoDiscoverEnemyStructuresFromTerritory(viewerFaction);const scouts=[...(viewer.units||[]),...(viewer.workers||[]),...(viewer.tanks||[]),...(viewer.launchers||[]),...(viewer.patriots||[]),...(viewer.supplies||[]),...(viewer.drones||[]),...(viewer.helicopters||[]),...(viewer.ships||[]),...(viewer.leaders||[]),...(viewer.villagers||[])].filter(e=>e&&e.hp>0&&DISCOVERABLE_SCOUT_TYPES.has(e.type));if(!scouts.length)return;const targets=[...(enemy.bases||[]),...(enemy.towers||[]),...(enemy.houses||[]),...((enemy.apartments)||[]),...((enemy.trainings)||[]),...(enemy.walls||[]),...(enemy.harbors||[]),...((enemy.silos)||[]),...buildSites.filter(s=>s&&s.faction===enemy.id)];for(const target of targets){if(!target)continue;const key=structureDiscoveryKey(target);if(!key||viewer.discoveredEnemyStructures.has(key))continue;const c=entityCenter(target);for(const scout of scouts){const sc=entityCenter(scout);const dx=c.x-sc.x,dy=c.y-sc.y;if(dx*dx+dy*dy<=STRUCTURE_DISCOVERY_RANGE_SQ){viewer.discoveredEnemyStructures.add(key);break;}}}}
function startNightTest(durationTicks=NIGHT_TEST_DURATION_TICKS,fromNetwork=false){const dur=Math.max(1,Math.round(durationTicks||NIGHT_TEST_DURATION_TICKS));dayNight.forceNightUntil=Math.max(dayNight.forceNightUntil||0,tick+dur);updateDayNightVisuals();log(`🌙 Test noci spuštěn na ${Math.ceil(dur / 60)} s`,'player-log');if(MP.active&&!fromNetwork)MP.sendCmd({action:'nightTest',duration:dur});}
function updateDayNightVisuals(){const night=isNightNow();if(threeFP&&threeFP.renderer){try{threeFP.renderer.toneMappingExposure=night?0.62:0.92;if(threeFP.renderer.domElement){threeFP.renderer.domElement.style.filter=night?'brightness(0.82) saturate(0.92)':'';}
if(threeFP.scene&&threeFP.scene.background&&threeFP.scene.background.isColor){threeFP.scene.background.set(night?0x09111a:0x2b527d);}
if(threeFP.sky&&threeFP.sky.material&&threeFP.sky.material.uniforms){const u=threeFP.sky.material.uniforms;if(u.colZenith&&u.colZenith.value)u.colZenith.value.set(night?0x09111a:0x2b527d);if(u.colMiddle&&u.colMiddle.value)u.colMiddle.value.set(night?0x14253a:0x3f76a8);if(u.colHorizon&&u.colHorizon.value)u.colHorizon.value.set(night?0x213146:0x8fb9db);}
if(threeFP.hemi)threeFP.hemi.intensity=night?0.22:0.52;if(threeFP.sun)threeFP.sun.intensity=night?0.24:1.05;if(threeFP.fill)threeFP.fill.intensity=night?0.14:0.22;}catch(_){}}}
function updateDayNightState(){const night=isNightNow();if(night!==dayNight.lastIsNight){dayNight.lastIsNight=night;log(night?'🌙 Noc — mapa je tmavší, jednotky mají kratší dostřel a radar vidí jen blízko tvých sil.':'☀️ Den — plný dohled a normální dostřel.','combat-log');}
updateDayNightVisuals();}
const AI_ASSAULT_REEVALUATE_TICKS=90;const AI_ASSAULT_PLAN_LIFETIME=60*160;const AI_ASSAULT_MAX_LATERAL=TILE*6.8;function aiWrapAng(a){return Math.atan2(Math.sin(a),Math.cos(a));}
function aiTargetCenter(target){if(!target)return{x:0,y:0};if(buildSites.includes(target))return buildSiteCenter(target);return entityCenter(target);}function aiGetPlayerTargets(){return[...player.bases,...player.units,...player.workers,...(player.villagers||[]),...player.tanks,...(player.launchers||[]),...player.drones,...player.helicopters,...(player.patriots||[]),...(player.supplies||[]),...player.ships,...player.bikes,...player.leaders,...player.towers,...player.walls,...player.houses,...(player.apartments||[]),...(player.trainings||[]),...player.harbors,...(player.rails||[]),...(player.cranes||[]),...(player.trains||[]),...(player.silos||[]),...(player.radars||[]),...buildSites.filter(s=>s&&s.faction==='player')].filter(t=>t&&t.hp>0);}function aiPlayerDefenseScore(){const towers=(player.towers||[]).filter(t=>t&&t.hp>0).length;const walls=(player.walls||[]).filter(w=>w&&w.hp>0).length;return towers*3+Math.min(12,walls*0.45);}function aiIsPlayerSiloActive(){return(player.silos||[]).some(s=>s&&s.hp>0)||buildSites.some(s=>s&&s.faction==='player'&&s.type==='silo'&&(s.hp||1)>0);}function aiCountNearbyResources(tx,ty,r){let c=0;for(const res of resourceTileCache){if(Math.max(Math.abs(res.tx-tx),Math.abs(res.ty-ty))<=r)c++;}return c;}function aiChooseExpansionTile(cfg,nextBaseIdx){if(!ai.bases.length)return null;let centerX=MAP_W/2,centerY=MAP_H/2;let refBase=ai.bases.reduce((best,b)=>{let d=Math.hypot(b.x/TILE-centerX,b.y/TILE-centerY);return d<Math.hypot(best.x/TILE-centerX,b.y/TILE-centerY)?b:best;},ai.bases[0]);let rbx=Math.round(refBase.x/TILE),rby=Math.round(refBase.y/TILE);let toCenterAngle=Math.atan2(centerY-rby,centerX-rbx);let bestTile=null,bestScore=-1e18;const tries=50+(cfg.expansionTries||0);for(let attempt=0;attempt<tries;attempt++){let angle=toCenterAngle+(Math.random()-0.5)*Math.PI*(0.85+Math.min(0.7,nextBaseIdx*0.14));let d2=18+Math.floor(Math.random()*(22+nextBaseIdx*5));let tx=Math.round(rbx+Math.cos(angle)*d2);let ty=Math.round(rby+Math.sin(angle)*d2);tx=Math.max(4,Math.min(tx,MAP_W-6));ty=Math.max(4,Math.min(ty,MAP_H-6));if(!map[ty]||map[ty][tx]!==0)continue;if(!isBuildAreaFree(tx,ty,2,2))continue;let playerNearest=999,ownNearest=999;for(const pb of player.bases){playerNearest=Math.min(playerNearest,Math.max(Math.abs(tx-Math.round(pb.x/TILE)),Math.abs(ty-Math.round(pb.y/TILE))));}for(const ab of ai.bases){ownNearest=Math.min(ownNearest,Math.max(Math.abs(tx-Math.round(ab.x/TILE)),Math.abs(ty-Math.round(ab.y/TILE))));}if(playerNearest<BASE_MIN_DIST_FROM_ENEMY)continue;if(ownNearest<14)continue;let res=aiCountNearbyResources(tx+1,ty+1,8);let shoreline=0;for(let yy=Math.max(1,ty-5);yy<=Math.min(MAP_H-2,ty+6);yy++)for(let xx=Math.max(1,tx-5);xx<=Math.min(MAP_W-2,tx+6);xx++)if(map[yy][xx]===1)shoreline++;let openness=0;for(let yy=Math.max(1,ty-4);yy<=Math.min(MAP_H-2,ty+5);yy++)for(let xx=Math.max(1,tx-4);xx<=Math.min(MAP_W-2,tx+5);xx++)if(map[yy][xx]===0)openness++;let centerDist=Math.hypot(tx-centerX,ty-centerY);let score=res*(cfg.expansionResourceBias||8)+Math.min(playerNearest,44)*(cfg.expansionDistanceBias||3)+Math.min(ownNearest,28)*1.6+shoreline*0.24+openness*0.08-centerDist*(cfg.expansionCenterBias||1.2);if(aiIsPlayerSiloActive())score+=Math.min(playerNearest,60)*0.6;if(score>bestScore){bestScore=score;bestTile={tx,ty};}}return bestTile;}function aiScoreStrategicTarget(attacker,target,plan){if(!target||target.hp<=0)return-1e9;const c=aiTargetCenter(target);const dx=c.x-attacker.x,dy=c.y-attacker.y;const d=Math.hypot(dx,dy);let score=0;const defenseScore=aiPlayerDefenseScore();const cfg=currentAICfg();score-=d*0.024;if(target.type==='base')score+=720;else if(target.type==='silo')score+=410+(cfg.siloTargetBias||0);else if(target.type==='radar')score+=300;else if(target.type==='training'||target.type==='apartment'||target.type==='harbor')score+=250;else if(target.type==='tower')score+=210;else if(target.type==='wall')score+=105;else if(target.type==='tank'||target.type==='launcher'||target.type==='helicopter'||target.type==='leader')score+=170;else if(target.type==='unit'||target.type==='worker'||target.type==='villager'||target.type==='drone'||target.type==='bike')score+=110;else if(target.type==='house')score+=95;if(target.type==='worker')score+=(cfg.workerHuntBias||0);if(target.type==='villager')score+=Math.round((cfg.workerHuntBias||0)*0.45);if(defenseScore<=4&&target.type==='base')score+=280;if(defenseScore<=2&&target.type==='base')score+=160;if(defenseScore<=3&&(target.type==='wall'||target.type==='tower'))score-=190;if(defenseScore>=6&&target.type==='base')score-=(cfg.smartnessBonus||0)*0.7;if(defenseScore>=6&&(target.type==='worker'||target.type==='training'||target.type==='radar'||target.type==='silo'))score+=(cfg.smartnessBonus||0);const role=(plan&&plan.role)||'line';if(role==='siege'){if(target.type==='base'||target.type==='tower'||target.type==='wall'||target.type==='harbor'||target.type==='silo'||target.type==='radar'||target.type==='training')score+=190;if(target.type==='worker'||target.type==='villager')score-=95;}else if(role==='harass'||role==='raider'){if(target.type==='worker'||target.type==='unit'||target.type==='villager'||target.type==='training'||target.type==='apartment'||target.type==='radar')score+=220+(cfg.workerHarassBias||0);if(target.type==='base')score+=90;if(target.type==='tower')score-=230;if(target.type==='wall')score-=160;}else if(role==='air'){if(target.type==='worker'||target.type==='training'||target.type==='apartment'||target.type==='radar'||target.type==='silo')score+=240+(cfg.workerHarassBias||0);if(target.type==='base')score+=145;if(target.type==='tower')score-=defenseScore>2?190:30;if(target.type==='wall')score-=180;}else if(role==='naval'){if(target.type==='base'||target.type==='harbor'||target.type==='tower'||target.type==='training'||target.type==='radar')score+=185;if(target.type==='wall')score-=80;}else if(role==='command'){if(target.type==='base'||target.type==='tower'||target.type==='training'||target.type==='radar'||target.type==='silo')score+=130+(cfg.smartnessBonus||0)*0.45;}else{if(target.type==='base')score+=110;if(target.type==='tower')score+=65;if(target.type==='worker'||target.type==='villager')score+=(cfg.workerHuntBias||0)*0.35;}if(plan&&Number.isFinite(plan.attackAngle)){const ox=Number.isFinite(plan.originX)?plan.originX:attacker.x;const oy=Number.isFinite(plan.originY)?plan.originY:attacker.y;const lx=Math.cos(plan.attackAngle),ly=Math.sin(plan.attackAngle);const rx=c.x-ox,ry=c.y-oy;const along=rx*lx+ry*ly;const cross=Math.abs(-rx*ly+ry*lx);score+=along*0.018-cross*(cfg.forceThreeProngs?0.010:0.013);}if(plan&&plan.preferBase&&target.type==='base')score+=140;return score;}function aiPickStrategicTarget(attacker,plan){const targets=aiGetPlayerTargets();if(!targets.length)return null;let best=null,bestScore=-1e18;for(const target of targets){const score=aiScoreStrategicTarget(attacker,target,plan);if(score>bestScore){bestScore=score;best=target;}}return best;}function aiSetRallyFromPlan(attacker,target,plan){if(!attacker||!target||!plan)return;const lateral=Math.max(-AI_ASSAULT_MAX_LATERAL,Math.min(AI_ASSAULT_MAX_LATERAL,Number(plan.lateral)||0));const ang=Number.isFinite(plan.attackAngle)?plan.attackAngle:Math.atan2(aiTargetCenter(target).y-attacker.y,aiTargetCenter(target).x-attacker.x);const baseDepth=attacker.type==='tank'?50:attacker.type==='launcher'?92:attacker.type==='leader'?62:attacker.type==='bike'?34:attacker.type==='ship'?130:attacker.type==='helicopter'?96:attacker.type==='drone'?68:78;const depth=baseDepth+Math.max(0,Number(plan.depth)||0);attacker.rallyOffset={x:Math.cos(ang+Math.PI)*depth-Math.sin(ang)*lateral,y:Math.sin(ang+Math.PI)*depth+Math.cos(ang)*lateral,done:false};}
function aiMaintainPressure(attacker){if(!attacker||attacker.hp<=0||attacker.faction!=='ai'||MP.active)return;const plan=attacker._aiAssault;if(!plan)return;if(plan.expiresAt&&tick>plan.expiresAt){attacker._aiAssault=null;return;}const needsTarget=!attacker.attackTarget||attacker.attackTarget.hp<=0||tick>=(plan.nextRetargetTick||0);if(!needsTarget)return;const next=aiPickStrategicTarget(attacker,plan);if(!next)return;const changed=attacker.attackTarget!==next;attacker.attackTarget=next;if('pendingAttack'in attacker)attacker.pendingAttack=next;if((attacker.type==='unit'||attacker.type==='tank'||attacker.type==='launcher')&&(changed||!attacker.rallyOffset||attacker.rallyOffset.done)){aiSetRallyFromPlan(attacker,next,plan);}if(aiPlayerDefenseScore()<=2&&next.type==='base'&&(attacker.type==='unit'||attacker.type==='tank'||attacker.type==='launcher'))attacker.rallyOffset=null;plan.nextRetargetTick=tick+AI_ASSAULT_REEVALUATE_TICKS+Math.floor(Math.random()*30);}

const FP_FOV=Math.PI/3;const FP_ROT_SPEED=0.045;const FP_MOVE_SPEED=2.6;const FP_MAX_RAYS=520;const FP_COLLISION_RADIUS=14;const FP_PLAYER_ATTACK_MULT=4;const FP_MOUSE_SENS=0.0026;const FP_PITCH_MIN=-0.80;const FP_PITCH_MAX=0.80;const FP_RENDER_BACKEND='three';const HELI_FP_MIN_ALT=0;const HELI_FP_MAX_ALT=420;const HELI_FP_CRUISE_ALT=190;const HELI_FP_TOUCHDOWN_ALT=32;const HELI_FP_CLIMB_SPEED=6.2;const HELI_FP_ATT_STIFF=0.085;const HELI_FP_ATT_DAMP=0.955;const HELI_FP_MAX_PITCH=0.13;const HELI_FP_MAX_ROLL=0.28;const HELI_FP_ACCEL=0.24;const HELI_FP_STRAFE_ACCEL=0.22;const HELI_FP_DRAG=0.94;const HELI_FP_ALT_HOLD=0.16;const HELI_FP_CAM_TILT=-2*Math.PI/180;const HELI_FP_ROCKET_RANGE=1080;const HELI_MOUSE_ROLL_GAIN=0.010;let fpMode=false;let fp={x:0,y:0,ang:0,pitch:0,mouseYawDelta:0,avatar:null,saved:null,fireHeld:false,firePressed:false,fireReleased:false,fireCooldown:0,fireCharging:false,fireCharge:0,chargeShow:0,attackTarget:null,prevCamera:{x:0,y:0},prevMode:'select',prevSelection:[]};let _fpWallGrid=null;let _fpWallGridVersion=-1;function fpBuildWallGrid(){_fpWallGrid=new Uint16Array(MAP_W*MAP_H);for(let ty=0;ty<MAP_H;ty++){for(let tx=0;tx<MAP_W;tx++){let t=map[ty][tx];let code=0;if(t===2)code=2;else if(t===8)code=8;else if(t===10)code=10;else if(t===4)code=4;else if(t===5)code=5;else if(t===6)code=6;else if(t===7)code=7;_fpWallGrid[ty*MAP_W+tx]=code;}}
function mark1(tx,ty,code){if(tx>=0&&tx<MAP_W&&ty>=0&&ty<MAP_H)_fpWallGrid[ty*MAP_W+tx]=code;}
function markBase(b,code){let tx=Math.round(b.x/TILE),ty=Math.round(b.y/TILE);for(let dy=0;dy<2;dy++)for(let dx=0;dx<2;dx++)mark1(tx+dx,ty+dy,code);}
for(let fac of[player,ai]){let off=fac.id==='player'?100:200;for(let b of fac.bases)markBase(b,off+1);for(let t of fac.towers)mark1(Math.round(t.x/TILE),Math.round(t.y/TILE),off+2);for(let h of fac.houses)mark1(Math.round(h.x/TILE),Math.round(h.y/TILE),off+3);for(let g of(fac.trainings||[]))mark1(Math.round(g.x/TILE),Math.round(g.y/TILE),off+7);for(let w of fac.walls)mark1(Math.round(w.x/TILE),Math.round(w.y/TILE),off+4);for(let h of fac.harbors)mark1(Math.round(h.x/TILE),Math.round(h.y/TILE),off+5);}
_fpWallGridVersion=_buildingChangeCounter;}
function fpWallCode(tx,ty){if(tx<0||tx>=MAP_W||ty<0||ty>=MAP_H)return 999;if(!_fpWallGrid||_fpWallGridVersion!==_buildingChangeCounter)fpBuildWallGrid();const prevX=fp.x,prevY=fp.y;return _fpWallGrid[ty*MAP_W+tx]||0;}
function fpIsBlockedCircle(wx,wy,r){const minTx=Math.floor((wx-r)/TILE),maxTx=Math.floor((wx+r)/TILE);const minTy=Math.floor((wy-r)/TILE),maxTy=Math.floor((wy+r)/TILE);for(let ty=minTy;ty<=maxTy;ty++){for(let tx=minTx;tx<=maxTx;tx++){if(fpWallCode(tx,ty)!==0)return true;}}
return false;}
function fpPickAvatarFromSelection(){if(!selection||selection.length===0)return null;for(let s of selection){if(!s||s.faction!=='player')continue;if(['unit','worker','tank','leader','bike','drone','helicopter','patriot','supply','launcher','crane'].includes(s.type))return s;}
return null;}
function fpSaveAvatarState(av){return{target:av?av.target:null,attackTarget:av?av.attackTarget:null,pendingAttack:av?av.pendingAttack:null,vx:av?(av.vx||0):0,vy:av?(av.vy||0):0,state:av?av.state:undefined,mineTarget:av?av.mineTarget:null,buildSite:av?av.buildSite:null,repairTarget:av?av.repairTarget:null,mineTimer:av?(av.mineTimer||0):0,carry:av&&av.carry!==undefined?av.carry:undefined,};}
function fpClearAvatarOrders(av){if(!av)return;av.target=null;av.attackTarget=null;av.pendingAttack=null;av.vx=0;av.vy=0;if(av.type==='worker'){av.state='idle';av.mineTarget=null;av.buildSite=null;av.repairTarget=null;av.mineTimer=0;}}
function fpRestoreAvatarState(av,saved){if(!av||!saved)return;av.target=saved.target??null;av.attackTarget=saved.attackTarget??null;av.pendingAttack=saved.pendingAttack??null;av.vx=saved.vx||0;av.vy=saved.vy||0;if(av.type==='worker'){if(saved.state!==undefined)av.state=saved.state;av.mineTarget=saved.mineTarget??null;av.buildSite=saved.buildSite??null;av.repairTarget=saved.repairTarget??null;av.mineTimer=saved.mineTimer||0;if(saved.carry!==undefined&&av.carry!==undefined)av.carry=saved.carry;}}
function fpEnter(){if(fpMode)return;let av=fpPickAvatarFromSelection();if(!av){log('⚠ Vyber nejdřív vlastní jednotku nebo jeřáb (LMB) a pak stiskni F1 nebo 1.','player-log');return;}
if(typeof clearKeysDown==='function')clearKeysDown();if(FP_RENDER_BACKEND==='three'&&(typeof THREE==='undefined'||!THREE)){log('❌ Three.js se nenačetlo (blokovaný CDN / offline / zabezpečený režim). Povolit internet pro CDN, nebo spusť soubor přes server.','player-log');console.warn('[FP] THREE je undefined – 3D FP nelze spustit.');return;}
fp.prevCamera={x:camera.x,y:camera.y};fp.mouseYawDelta=0;fp.prevMode=mode;fp.prevSelection=selection.slice();fp.avatar=av;fp.saved=fpSaveAvatarState(fp.avatar);let ec=entityCenter(fp.avatar);fp.x=ec.x;fp.y=ec.y;const svx=fp.saved.vx||0;const svy=fp.saved.vy||0;fp.ang=(Math.hypot(svx,svy)>0.12)?Math.atan2(svy,svx):(fp.ang||0);fpMode=true;setMobileFPLayoutActive(mobileView.enabled);fpBuildWallGrid();if(FP_RENDER_BACKEND==='three'){try{threeFPEnable(true);}catch(err){console.error('[FP] threeFPEnable selhalo:',err);fpMode=false;try{threeFPDisable();}catch(_){}
canvas.style.display='block';const th=document.getElementById('three-holder');const fui=document.getElementById('fp-ui');if(th)th.style.display='none';if(fui)fui.style.display='none';fp.avatar=null;fp.saved=null;log('❌ Nepovedlo se spustit 3D FIRST PERSON (WebGL/Three.js error). Koukej do Console (F12).','player-log');return;}}
fpClearAvatarOrders(fp.avatar);setModeDisplay('FIRST PERSON (Esc = zpět)');fpTryPointerLock();try{updateMobileFPButton();}catch(_){}if(mobileView.enabled)scheduleMobileFPHintHide(1800);log('👁 FIRST PERSON (3D): WASD = pohyb + úkroky | myš = rozhlížení | LMB = útok | Esc = zpět | F1/1 = přepnout','player-log');}
function fpExit(){if(!fpMode)return;try{mobileFPResetJoysticks();}catch(_){}if(typeof clearKeysDown==='function')clearKeysDown();const av=fp.avatar;if(av&&av.hp>0&&fp.saved){fpRestoreAvatarState(av,fp.saved);}
fp.saved=null;fpMode=false;setMobileFPLayoutActive(false);const fpHintEl=document.getElementById('fp-hint');if(fpHintEl){fpHintEl.style.display='block';fpHintEl.style.opacity='1';}if(FP_RENDER_BACKEND==='three')threeFPDisable();if(av){camera.x=av.x-canvas.width/2;camera.y=av.y-canvas.height/2;}else{camera.x=fp.prevCamera.x;camera.y=fp.prevCamera.y;}
clampCamera();mode=fp.prevMode||'select';selection=(fp.prevSelection&&fp.prevSelection.length)?fp.prevSelection.slice():selection;fp.avatar=null;setModeDisplay(mode==='select'?'Výběr':'Režim');try{scheduleMobileFooterHintHide(1200);}catch(_){}try{updateMobileTouchHud();}catch(_){}}
function fpToggle(){if(fpMode)fpExit();else fpEnter();}
function fpTryPointerLock(){if(FP_RENDER_BACKEND!=='three')return;if(!threeFP||!threeFP.renderer)return;const el=threeFP.renderer.domElement;if(!el||!el.requestPointerLock)return;if(document.pointerLockElement!==el){try{el.requestPointerLock();}catch(_){}}}
function fpOnMouseMove(e){if(!fpMode)return;if(FP_RENDER_BACKEND!=='three')return;if(!threeFP||!threeFP.renderer)return;const el=threeFP.renderer.domElement;if(document.pointerLockElement!==el)return;const mx=(e.movementX||0);fp.ang+=mx*FP_MOUSE_SENS;fp.mouseYawDelta=(fp.mouseYawDelta||0)+mx*FP_MOUSE_SENS;fp.pitch-=(e.movementY||0)*FP_MOUSE_SENS;fp.pitch=Math.max(FP_PITCH_MIN,Math.min(FP_PITCH_MAX,fp.pitch));}
function fpOnMouseDown(e){if(!fpMode)return;if(e.button!==0)return;e.preventDefault();e.stopPropagation();fp.fireHeld=true;fp.firePressed=true;fp.fireReleased=false;fp.fireCharging=true;fp.fireCharge=0;fp.chargeShow=0;fpTryPointerLock();}
function fpOnMouseUp(e){if(!fpMode)return;if(e.button!==0)return;e.preventDefault();e.stopPropagation();fp.fireHeld=false;fp.firePressed=false;fp.fireReleased=true;}
document.addEventListener('pointerlockchange',()=>{});window.addEventListener('mousemove',fpOnMouseMove,{passive:false});window.addEventListener('mousedown',fpOnMouseDown,{passive:false});window.addEventListener('mouseup',fpOnMouseUp,{passive:false});window.addEventListener('contextmenu',e=>{if(fpMode){e.preventDefault();e.stopPropagation();}},{passive:false});function fpGetWeaponStats(av){if(!av)return null;if(av.type==='tank')return{kind:'shell',range:getNightCombatRange(av,TANK_RANGE),damage:TANK_DAMAGE,fireRate:TANK_FIRE_RATE,splash:TANK_SPLASH,inacc:TANK_INACCURACY};if(av.type==='launcher')return{kind:'rocket',range:getNightCombatRange(av,LAUNCHER_RANGE),minRange:LAUNCHER_MIN_RANGE,damage:LAUNCHER_DAMAGE,fireRate:LAUNCHER_FIRE_RATE,splash:LAUNCHER_SPLASH,inacc:LAUNCHER_INACCURACY};if(av.type==='ship')return{kind:'shell',range:getNightCombatRange(av,SHIP_RANGE),damage:SHIP_DAMAGE,fireRate:SHIP_FIRE_RATE,splash:SHIP_SPLASH,inacc:SHIP_INACCURACY};if(av.type==='bike')return{kind:'bullet',range:BIKE_RANGE,damage:BIKE_DAMAGE,fireRate:BIKE_FIRE_RATE};if(av.type==='drone')return{kind:'bullet',range:getNightCombatRange(av,DRONE_RANGE),damage:DRONE_DAMAGE,fireRate:DRONE_FIRE_RATE};if(av.type==='helicopter'){const heliRange=(fpMode&&fp.avatar===av)?HELI_FP_ROCKET_RANGE:HELICOPTER_RANGE;return{kind:'rocket',range:getNightCombatRange(av,heliRange),damage:HELICOPTER_DAMAGE,fireRate:HELICOPTER_FIRE_RATE};}
if(av.type==='unit')return{kind:'bullet',range:getNightCombatRange(av,UNIT_RANGE),damage:UNIT_DAMAGE,fireRate:UNIT_FIRE_RATE};return null;}
function fpGetShootRayRTS(stats){let flatDx=Math.cos(fp.ang),flatDy=Math.sin(fp.ang);let groundHit=null;if(FP_RENDER_BACKEND==='three'&&threeFP&&threeFP.enabled&&threeFP.camera){const dir=new THREE.Vector3();threeFP.camera.getWorldDirection(dir);let dx=dir.x/threeFP.scale;let dy=dir.z/threeFP.scale;let dl=Math.hypot(dx,dy);if(dl>1e-6){dx/=dl;dy/=dl;flatDx=dx;flatDy=dy;}
if(dir.y<-0.001){const o=threeFP.camera.position;const t=o.y/(-dir.y);const maxT=(stats&&stats.range?stats.range:120)*threeFP.scale;if(t>0&&t<=maxT*1.25){const hx=o.x+dir.x*t;const hz=o.z+dir.z*t;const gx=hx/threeFP.scale;const gy=hz/threeFP.scale;groundHit={x:gx,y:gy};}}}
return{flatDx,flatDy,groundHit};}
function getHelicopterBombImpactPoint(h,aimOverride){const from=entityCenter(h);const aim=Number.isFinite(aimOverride)?aimOverride:(h._aimAng!=null?h._aimAng:(Math.hypot(h.vx||0,h.vy||0)>0.08?Math.atan2(h.vy||0,h.vx||0):0));const forward=34;const lead=Math.min(10,Math.hypot(h.vx||0,h.vy||0)*5.5);const ex=Math.max(6,Math.min(MAP_W*TILE-6,from.x+Math.cos(aim)*(forward+lead)));const ey=Math.max(6,Math.min(MAP_H*TILE-6,from.y+Math.sin(aim)*(forward+lead)));return{x:ex,y:ey,from,aim};}
function getHelicopterRocketCandidates(faction){const enemy=faction==='player'?ai:player;const enemyFaction=faction==='player'?'ai':'player';return[...(enemy.units||[]),...(enemy.workers||[]),...(enemy.villagers||[]),...(enemy.tanks||[]),...(enemy.drones||[]),...(enemy.helicopters||[]),...(enemy.bikes||[]),...(enemy.leaders||[]),...(enemy.ships||[]),...(enemy.towers||[]),...(enemy.houses||[]),...(enemy.apartments||[]),...(enemy.trainings||[]),...(enemy.harbors||[]),...(enemy.walls||[]),...(enemy.bases||[]),...buildSites.filter(s=>s&&s.faction===enemyFaction)].filter(Boolean);}
function getHelicopterTargetCenter(e){if(!e)return null;if(buildSites.includes(e))return buildSiteCenter(e);return entityCenter(e);}
function getHelicopterHitRadius(e){if(!e)return 10;if(e.type==='base'||e.w===2||e.h===2)return 22;if(e.type==='tower'||e.type==='tank'||e.type==='ship'||e.type==='harbor'||e.type==='silo')return 16;if(e.type==='house'||e.type==='apartment'||e.type==='training'||e.type==='wall')return 18;if(e.type==='helicopter')return 14;if(e.type==='worker'||e.type==='unit'||e.type==='villager'||e.type==='leader'||e.type==='bike')return 11;if(e.type==='drone')return 8;return 10;}
function findHelicopterRocketTarget(h,from,aimX,aimY,preferredTarget,rangeLimit){const candidates=getHelicopterRocketCandidates(h.faction);const maxRange=Math.max(8,rangeLimit||HELICOPTER_RANGE);let best=null,bestScore=1e18;const dxAim=aimX-from.x,dyAim=aimY-from.y;const lenAim=Math.max(1,Math.hypot(dxAim,dyAim));const ax=dxAim/lenAim,ay=dyAim/lenAim;if(preferredTarget&&preferredTarget.hp>0&&preferredTarget.faction!==h.faction){const pc=getHelicopterTargetCenter(preferredTarget);if(pc){const pd=Math.hypot(pc.x-aimX,pc.y-aimY);const fd=Math.hypot(pc.x-from.x,pc.y-from.y);if(fd<=maxRange*1.06&&pd<=getHelicopterHitRadius(preferredTarget)+28)return preferredTarget;}}
for(const e of candidates){if(!e||e.hp<=0||e.faction===h.faction)continue;const c=getHelicopterTargetCenter(e);if(!c)continue;const ex=c.x-from.x,ey=c.y-from.y;const along=ex*ax+ey*ay;if(along<-8||along>maxRange*1.08)continue;const perp=Math.abs(-ex*ay+ey*ax);const radius=getHelicopterHitRadius(e)+(e.type==='base'||e.type==='house'||e.type==='apartment'||e.type==='training'||e.type==='harbor'||e.type==='wall'?4:0);const impactDist=Math.hypot(c.x-aimX,c.y-aimY);if(impactDist>radius+34&&perp>radius+18)continue;const score=impactDist*1.55+perp*0.95+along*0.012-radius*1.8;if(score<bestScore){bestScore=score;best=e;}}
return best;}
function fireHelicopterRocket(h,aimX,aimY,preferredTarget,opts){if(!h||h.hp<=0||h.type!=='helicopter')return false;const o=opts||{};const maxRange=Math.max(8,o.range||HELICOPTER_RANGE);const from=getHelicopterShotOrigin2D(h);const visualFrom=getHelicopterShotVisualOrigin2D(h);let dx=aimX-from.x,dy=aimY-from.y;let d=Math.hypot(dx,dy);if(d<0.001){const a0=h._aimAng!=null?h._aimAng:(Math.hypot(h.vx||0,h.vy||0)>0.08?Math.atan2(h.vy||0,h.vx||0):0);dx=Math.cos(a0)*maxRange;dy=Math.sin(a0)*maxRange;d=maxRange;}
if(d>maxRange){aimX=from.x+dx/d*maxRange;aimY=from.y+dy/d*maxRange;d=maxRange;}
h._aimAng=Math.atan2(aimY-from.y,aimX-from.x);h._aimT=18;const direct=findHelicopterRocketTarget(h,from,aimX,aimY,preferredTarget,maxRange);const impact=direct?getHelicopterTargetCenter(direct):{x:aimX,y:aimY};const impact3=o.impact3||(direct?getHelicopterTargetWorldPoint(direct):null);const muzzle3=o.muzzle3||null;spawnShotFx(h.faction,visualFrom.x,visualFrom.y,impact.x,impact.y,'rocket',h,{v1:muzzle3||undefined,v2:impact3||undefined,muzzle3:muzzle3||undefined});AudioRTS.shot('heli');if(direct)dealDamage(direct,HELICOPTER_DAMAGE,h);const splash=HELICOPTER_ROCKET_SPLASH;const splashDamage=HELICOPTER_ROCKET_SPLASH_DAMAGE;if(splashDamage>0){for(const e of getHelicopterRocketCandidates(h.faction)){if(!e||e===direct||e.hp<=0||e.faction===h.faction)continue;const c=getHelicopterTargetCenter(e);const radius=getHelicopterHitRadius(e)*0.35;if(Math.hypot(c.x-impact.x,c.y-impact.y)<=splash+radius)dealDamage(e,splashDamage,h);}}
for(let i=0;i<5;i++){const ang=(i/5)*Math.PI*2+Math.random()*0.35;const r=5+Math.random()*9;explosions.push({x:impact.x,y:impact.y,dx:Math.cos(ang)*r,dy:Math.sin(ang)*r,life:10,maxLife:10});}
try{if(FP_RENDER_BACKEND==='three'&&threeFP&&threeFP.enabled){}}catch(_){}
return true;}
function dropHelicopterBomb(h){if(!h||h.hp<=0||h.type!=='helicopter')return false;h.bombCooldown=Math.max(0,h.bombCooldown||0);if(h.bombCooldown>0)return false;const{x:ex,y:ey,from}=getHelicopterBombImpactPoint(h);h.bombCooldown=HELICOPTER_BOMB_COOLDOWN;AudioRTS.shot('bomb');explodeAt(ex,ey,h.faction,HELICOPTER_BOMB_SPLASH,HELICOPTER_BOMB_DAMAGE,h);const vFrom=getHelicopterShotVisualOrigin2D(h);spawnShotFx(h.faction,vFrom.x,vFrom.y,ex,ey,'bomb',h);return true;}
function fpDropHeliBomb(){if(!fpMode||!fp.avatar||fp.avatar.type!=='helicopter')return false;const h=fp.avatar;h.bombCooldown=Math.max(0,h.bombCooldown||0);if(h.bombCooldown>0)return false;const{x:ex,y:ey,from}=getHelicopterBombImpactPoint(h,fp.ang);h.bombCooldown=HELICOPTER_BOMB_COOLDOWN;AudioRTS.shot('bomb');explodeAt(ex,ey,h.faction,HELICOPTER_BOMB_SPLASH,HELICOPTER_BOMB_DAMAGE,h);spawnShotFx(h.faction,from.x,from.y,ex,ey,'bomb',h,(FP_RENDER_BACKEND==='three'&&threeFP&&threeFP.enabled)?{v1:getHelicopterMuzzleWorld(h,fp.ang),v2:(()=>{const p=threeFPWorldPos(ex,ey);const gy=(threeFP.groundHeight?threeFP.groundHeight(p.x,p.z):0);return new THREE.Vector3(p.x,gy+0.08,p.z);})(),muzzle3:getHelicopterMuzzleWorld(h,fp.ang)}:undefined);try{if(FP_RENDER_BACKEND==='three'&&threeFP&&threeFP.enabled){}}catch(_){}
return true;}
function fpGetHelicopterRocketAim(stats){const from={x:fp.avatar.x,y:fp.avatar.y};let target=null;let aimPoint=null;let impact3=null;if(FP_RENDER_BACKEND==='three'&&threeFP&&threeFP.enabled&&threeFP.camera){const lock=fpHelicopterLockTarget(stats.range);if(lock){target=lock.entity;aimPoint=lock.aimPoint;impact3=lock.point3;}
if(!target){const hit=threeFPRaycastCenter();if(hit&&hit.entity&&hit.entity.faction!==fp.avatar.faction&&hit.entity.hp>0&&hit.dist<=stats.range*threeFP.scale*1.18){target=hit.entity;aimPoint=getHelicopterTargetCenter(target);impact3=getHelicopterTargetWorldPoint(target);}}
if(!aimPoint){const dir=new THREE.Vector3();threeFP.camera.getWorldDirection(dir);const muzzle=getHelicopterMuzzleWorld(fp.avatar,fp.ang);const surf=threeFPRaycastSurface(muzzle,dir,stats.range*threeFP.scale*1.18);if(surf&&surf.point){aimPoint={x:surf.point.x/threeFP.scale,y:surf.point.z/threeFP.scale};impact3=surf.point.clone();}}}
if(!target)target=fpHelicopterAimAssist(stats.range)||fpFindTargetAlongRay(from,fp.ang,stats.range*1.10,'helicopter');if(target&&(!aimPoint)){aimPoint=getHelicopterTargetCenter(target);impact3=getHelicopterTargetWorldPoint(target);}
if(!aimPoint){const ray=fpGetShootRayRTS(stats);aimPoint=ray.groundHit||{x:from.x+ray.flatDx*stats.range,y:from.y+ray.flatDy*stats.range};}
return{aimPoint,target,impact3};}
function fpFireOnce(stats,charge){if(!fpMode||!fp.avatar||!stats)return false;const ch=(charge===undefined||charge===null)?0.0:Math.max(0,Math.min(1,charge));if(fp.avatar.type==='helicopter'){const aim=fpGetHelicopterRocketAim(stats);if(!aim||!aim.aimPoint)return false;const muzzle3=(FP_RENDER_BACKEND==='three'&&threeFP&&threeFP.enabled)?getHelicopterMuzzleWorld(fp.avatar,fp.ang):null;return fireHelicopterRocket(fp.avatar,aim.aimPoint.x,aim.aimPoint.y,aim.target,{range:stats.range,impact3:aim.impact3||undefined,muzzle3:muzzle3||undefined});}if(fp.avatar.type==='launcher'){const from={x:fp.avatar.x,y:fp.avatar.y};let target=fpFindTargetAlongRay(from,fp.ang,stats.range,'launcher');let aimPoint=target?entityCenter(target):null;if(!aimPoint&&FP_RENDER_BACKEND==='three'&&threeFP&&threeFP.enabled&&threeFP.camera){const dir=new THREE.Vector3();threeFP.camera.getWorldDirection(dir);if(dir.y<-0.001){const o=threeFP.camera.position;const t=o.y/(-dir.y);const maxT=stats.range*threeFP.scale;if(t>0&&t<=maxT*1.2){const hx=o.x+dir.x*t,hz=o.z+dir.z*t;aimPoint={x:hx/threeFP.scale,y:hz/threeFP.scale};}}}if(!aimPoint)aimPoint={x:from.x+Math.cos(fp.ang)*Math.max(stats.minRange+8,Math.min(stats.range,160)),y:from.y+Math.sin(fp.ang)*Math.max(stats.minRange+8,Math.min(stats.range,160))};let dx=aimPoint.x-from.x,dy=aimPoint.y-from.y;let d=Math.hypot(dx,dy);if(d<stats.minRange)return false;if(d>stats.range&&d>1e-6){const s=stats.range/d;aimPoint={x:from.x+dx*s,y:from.y+dy*s};dx=aimPoint.x-from.x;dy=aimPoint.y-from.y;d=stats.range;}fp.avatar._aimAng=Math.atan2(dy,dx);fp.avatar._aimT=18;return !!fireRocketShell({x:from.x,y:from.y,_att:fp.avatar},aimPoint.x,aimPoint.y,fp.avatar.faction,stats.splash,stats.damage,stats.inacc);}
const from=entityCenter(fp.avatar);let endX=from.x+Math.cos(fp.ang)*stats.range;let endY=from.y+Math.sin(fp.ang)*stats.range;let hit=null;if(FP_RENDER_BACKEND==='three'&&threeFP&&threeFP.enabled&&threeFP.camera){const dir=new THREE.Vector3();threeFP.camera.getWorldDirection(dir);const camO=threeFP.camera.position.clone();const muzzle=camO.clone().add(dir.clone().multiplyScalar(0.55));muzzle.y-=0.10;const maxDistW=stats.range*threeFP.scale;hit=threeFPRaycastCenter();if((!hit||!hit.entity||hit.entity.faction===fp.avatar.faction||hit.entity.hp<=0)&&fp.avatar.type==='helicopter'){const assisted=fpHelicopterAimAssist(stats.range);if(assisted){const ec=entityCenter(assisted);const wp=threeFPWorldPos(ec.x,ec.y);const gyHit=(threeFP.groundHeight?threeFP.groundHeight(wp.x,wp.z):0);const point3=new THREE.Vector3(wp.x,gyHit+(assisted.type==='helicopter'?((assisted.alt||HELICOPTER_ALTITUDE)/TILE)+1.8:0.95),wp.z);const dxw=point3.x-muzzle.x,dyw=point3.y-muzzle.y,dzw=point3.z-muzzle.z;const distW=Math.hypot(dxw,dyw,dzw);hit={entity:assisted,pointWorld:{wx:ec.x,wy:ec.y},point3,dist:distW};}}
if((!hit||!hit.entity||hit.entity.faction===fp.avatar.faction||hit.entity.hp<=0||hit.dist>maxDistW*1.08)){const fallbackEnt=fpFindTargetAlongRay(from,fp.ang,stats.range,fp.avatar.type);if(fallbackEnt){const ec=entityCenter(fallbackEnt);const wp=threeFPWorldPos(ec.x,ec.y);const gyHit=(threeFP.groundHeight?threeFP.groundHeight(wp.x,wp.z):0);const point3=new THREE.Vector3(wp.x,gyHit+((fallbackEnt.type==='helicopter')?(((fallbackEnt.alt||HELICOPTER_ALTITUDE)/TILE)+1.8):(threeFPUnitBarHeight(fallbackEnt)*0.45)),wp.z);const dxw=point3.x-muzzle.x,dyw=point3.y-muzzle.y,dzw=point3.z-muzzle.z;const distW=Math.hypot(dxw,dyw,dzw);if(distW<=maxDistW*1.08)hit={entity:fallbackEnt,pointWorld:{wx:ec.x,wy:ec.y},point3,dist:distW};}}
let endWorld=muzzle.clone().add(dir.clone().multiplyScalar(maxDistW));let hasImpact=false;if(hit&&hit.entity&&fp.avatar&&hit.entity.faction!==fp.avatar.faction&&hit.entity.hp>0){if(hit.dist<=maxDistW*1.08&&hit.point3){endWorld.copy(hit.point3);hasImpact=true;}}else{const surf=threeFPRaycastSurface(muzzle,dir,maxDistW);if(surf&&surf.point){endWorld.copy(surf.point);hasImpact=true;}}
endX=endWorld.x/threeFP.scale;endY=endWorld.z/threeFP.scale;fp.avatar._aimAng=Math.atan2(endY-from.y,endX-from.x);fp.avatar._aimT=16;try{const css=(fp.avatar.faction==='player'?player.color:ai.color);threeFPSpawnTracer(from.x,from.y,endX,endY,css,{v1:muzzle,v2:endWorld,off:0.0,thick:0.9+ch*0.8});threeFPSpawnBullet(from.x,from.y,endX,endY,css,(stats.kind||'bullet'),{v1:muzzle,v2:endWorld,kind:(stats.kind||'bullet'),charge:ch});threeFPSpawnFlash(from.x+Math.cos(fp.avatar._aimAng)*18,from.y+Math.sin(fp.avatar._aimAng)*18,css,0.75+ch*0.55,10);if(threeFP.fx&&threeFP.fx.smokeSys){threeFPEmitParticle(threeFP.fx.smokeSys,muzzle.x,muzzle.y,muzzle.z,{kind:'smoke',life:26+Math.random()*14,size:20+Math.random()*10,r:0.55,g:0.56,b:0.58,a:0.22,vx:(Math.random()-0.5)*0.004,vy:0.006+Math.random()*0.004,vz:(Math.random()-0.5)*0.004});}
if(hasImpact)threeFPSpawnImpact(endX,endY,css,(stats.kind||'bullet'),{point3:endWorld});}catch(_){}
if(hit&&hit.entity&&fp.avatar&&hit.entity.faction!==fp.avatar.faction&&hit.entity.hp>0&&hit.dist<=maxDistW*1.08){const dmg=stats.damage*(1.0+0.6*ch);if(stats.kind==='shell'){fireShell({x:from.x,y:from.y,_att:fp.avatar,_no3dFx:true},endX,endY,fp.avatar.faction,stats.splash,dmg,stats.inacc);}else{AudioRTS.shot(fp.avatar.type==='drone'?'drone':fp.avatar.type==='bike'?'bike':'rifle');dealDamage(hit.entity,dmg,fp.avatar);if(fp.avatar.type==='helicopter'){const near=fpFindTargetAlongRay(from,fp.avatar._aimAng||fp.ang,Math.min(stats.range,26),'helicopter');if(near&&near!==hit.entity&&near.faction!==fp.avatar.faction)dealDamage(near,dmg*0.35,fp.avatar);}}}else{if(stats.kind==='shell'){const dmg=stats.damage*(1.0+0.6*ch);fireShell({x:from.x,y:from.y,_att:fp.avatar,_no3dFx:true},endX,endY,fp.avatar.faction,stats.splash,dmg,stats.inacc);}else{AudioRTS.shot(fp.avatar.type==='drone'?'drone':fp.avatar.type==='bike'?'bike':'rifle');}}
return true;}
if(FP_RENDER_BACKEND==='three'&&threeFP&&threeFP.enabled){hit=threeFPRaycastCenter();}
if(hit&&hit.entity&&fp.avatar&&hit.entity.faction!==fp.avatar.faction&&hit.entity.hp>0){const hx=hit.pointWorld?.wx??entityCenter(hit.entity).x;const hy=hit.pointWorld?.wy??entityCenter(hit.entity).y;const dH=Math.hypot(hx-from.x,hy-from.y);if(dH<=stats.range){endX=hx;endY=hy;}}
fp.avatar._aimAng=Math.atan2(endY-from.y,endX-from.x);fp.avatar._aimT=16;if(stats.kind==='shell'){const dmg=stats.damage*(1.0+0.6*ch);fireShell({x:from.x,y:from.y,_att:fp.avatar},endX,endY,fp.avatar.faction,stats.splash,dmg,stats.inacc);}else{AudioRTS.shot(fp.avatar.type==='drone'?'drone':fp.avatar.type==='bike'?'bike':'rifle');if(hit&&hit.entity&&hit.entity.faction!==fp.avatar.faction&&hit.entity.hp>0)dealDamage(hit.entity,stats.damage*(1.0+0.6*ch),fp.avatar);spawnShotFx(fp.avatar.faction,from.x,from.y,endX,endY,'bullet',fp.avatar);}
return true;}
function fpUpdateMovement(){if(!fpMode)return;if(!fp.avatar||fp.avatar.hp<=0){fpExit();return;}
if(!_fpWallGrid||_fpWallGridVersion!==_buildingChangeCounter)fpBuildWallGrid();const prevAng=fp.ang;const moveJoyX=(mobileView.enabled&&fpMode)?(mobileView.fpMoveX||0):0;const moveJoyY=(mobileView.enabled&&fpMode)?(mobileView.fpMoveY||0):0;const lookJoyX=(mobileView.enabled&&fpMode)?(mobileView.fpLookX||0):0;const lookJoyY=(mobileView.enabled&&fpMode)?(mobileView.fpLookY||0):0;if(keysDown['ArrowLeft']||keysDown['KeyQ'])fp.ang-=FP_ROT_SPEED;if(keysDown['ArrowRight']||keysDown['KeyE'])fp.ang+=FP_ROT_SPEED;if(Math.abs(lookJoyX)>0.02)fp.ang+=lookJoyX*(FP_ROT_SPEED*1.95);if(Math.abs(lookJoyY)>0.02){fp.pitch=Math.max(FP_PITCH_MIN,Math.min(FP_PITCH_MAX,fp.pitch+lookJoyY*(FP_MOUSE_SENS*26)));}const fwd=(keysDown['KeyW']||keysDown['w']||keysDown['W']||keysDown['ArrowUp'])?1:0;const back=(keysDown['KeyS']||keysDown['s']||keysDown['S']||keysDown['ArrowDown'])?1:0;const left=(keysDown['KeyA']||keysDown['a']||keysDown['A'])?1:0;const right=(keysDown['KeyD']||keysDown['d']||keysDown['D'])?1:0;let move=(fwd-back)+(-moveJoyY);let strafe=(right-left)+moveJoyX;move=Math.max(-1,Math.min(1,move));strafe=Math.max(-1,Math.min(1,strafe));if(move!==0&&strafe!==0){const inv=1/Math.sqrt(2);move*=inv;strafe*=inv;}
let sp=FP_MOVE_SPEED;if(fp.avatar){if(fp.avatar.type==='unit')sp=UNIT_SPEED;else if(fp.avatar.type==='worker')sp=WORKER_SPEED;else if(fp.avatar.type==='tank')sp=TANK_SPEED;else if(fp.avatar.type==='launcher')sp=LAUNCHER_SPEED;else if(fp.avatar.type==='bike')sp=BIKE_SPEED;else if(fp.avatar.type==='drone')sp=DRONE_SPEED;else if(fp.avatar.type==='helicopter')sp=HELICOPTER_SPEED;else if(fp.avatar.type==='ship')sp=SHIP_SPEED;else if(fp.avatar.type==='leader')sp=LEADER_SPEED;else if(fp.avatar.type==='patriot')sp=PATRIOT_SPEED;else if(fp.avatar.type==='supply')sp=SUPPLY_SPEED;else if(fp.avatar.type==='crane')sp=CRANE_MOVE_SPEED;else if(fp.avatar.type==='train')sp=TRAIN_MOVE_SPEED;}
if(fp.avatar&&fp.avatar.type!=='helicopter'){if(isWaterAt(fp.x,fp.y)){if(fp.avatar.type==='tank')sp=TANK_WATER_SPEED;else if(fp.avatar.type==='launcher')sp=LAUNCHER_WATER_SPEED;else if(fp.avatar.type==='bike')sp=BIKE_WATER_SPEED;else sp*=0.25;}
if(isSoftObstacle(fp.x,fp.y))sp*=0.6;}
const cosA=Math.cos(fp.ang),sinA=Math.sin(fp.ang);let dx=(cosA*move+(-sinA)*strafe)*sp;let dy=(sinA*move+(cosA)*strafe)*sp;if(fp.avatar&&fp.avatar.type==='helicopter'){fp.avatar._fpVelF=fp.avatar._fpVelF||0;fp.avatar._fpVelR=fp.avatar._fpVelR||0;fp.avatar._fpAlt=(fp.avatar._fpAlt==null?Math.max(HELI_FP_CRUISE_ALT,fp.avatar.alt||HELI_FP_CRUISE_ALT):fp.avatar._fpAlt);fp.avatar._fpTargetAlt=(fp.avatar._fpTargetAlt==null?fp.avatar._fpAlt:fp.avatar._fpTargetAlt);fp.avatar._fpPitch=fp.avatar._fpPitch||0;fp.avatar._fpRoll=fp.avatar._fpRoll||0;const mouseYaw=fp.mouseYawDelta||0;const yawDelta=Math.atan2(Math.sin(fp.ang-prevAng),Math.cos(fp.ang-prevAng));const turnCmd=Math.max(-1,Math.min(1,yawDelta/0.060));const pitchTarget=-move*HELI_FP_MAX_PITCH;const mouseBank=Math.max(-HELI_FP_MAX_ROLL*0.32,Math.min(HELI_FP_MAX_ROLL*0.32,mouseYaw*(HELI_MOUSE_ROLL_GAIN/Math.max(0.0001,FP_MOUSE_SENS))*0.55));const rollTarget=Math.max(-HELI_FP_MAX_ROLL*0.88,Math.min(HELI_FP_MAX_ROLL*0.88,-strafe*HELI_FP_MAX_ROLL*0.36+turnCmd*HELI_FP_MAX_ROLL*0.28+mouseBank));fp.avatar._fpPitch+=(pitchTarget-fp.avatar._fpPitch)*HELI_FP_ATT_STIFF;fp.avatar._fpRoll+=(rollTarget-fp.avatar._fpRoll)*HELI_FP_ATT_STIFF;fp.avatar._fpPitch*=HELI_FP_ATT_DAMP;fp.avatar._fpRoll*=HELI_FP_ATT_DAMP;fp.avatar._fpPitch=Math.max(-HELI_FP_MAX_PITCH,Math.min(HELI_FP_MAX_PITCH,fp.avatar._fpPitch));fp.avatar._fpRoll=Math.max(-HELI_FP_MAX_ROLL,Math.min(HELI_FP_MAX_ROLL,fp.avatar._fpRoll));fp.avatar._fpVelF+=((move*sp)-fp.avatar._fpVelF)*HELI_FP_ACCEL;fp.avatar._fpVelR+=((strafe*sp)-fp.avatar._fpVelR)*HELI_FP_STRAFE_ACCEL;fp.avatar._fpVelF*=HELI_FP_DRAG;fp.avatar._fpVelR*=HELI_FP_DRAG;const up=(keysDown['PageUp']||keysDown['KeyR']||keysDown['r']||keysDown['R'])?1:0;const down=(keysDown['PageDown']||keysDown['KeyF']||keysDown['f']||keysDown['F']||keysDown['ShiftLeft']||keysDown['ShiftRight'])?1:0;if(up||down)fp.avatar._fpTargetAlt+=(up-down)*HELI_FP_CLIMB_SPEED;fp.avatar._fpTargetAlt=Math.max(HELI_FP_MIN_ALT,Math.min(HELI_FP_MAX_ALT,fp.avatar._fpTargetAlt));fp.avatar._fpAlt+=(fp.avatar._fpTargetAlt-fp.avatar._fpAlt)*HELI_FP_ALT_HOLD;fp.avatar._fpAlt=Math.max(HELI_FP_MIN_ALT,Math.min(HELI_FP_MAX_ALT,fp.avatar._fpAlt));const dxHeli=(cosA*fp.avatar._fpVelF+(-sinA)*fp.avatar._fpVelR);const dyHeli=(sinA*fp.avatar._fpVelF+(cosA)*fp.avatar._fpVelR);const landed=fp.avatar._fpAlt<=6;if(landed){let nx=fp.x+dxHeli,ny=fp.y+dyHeli;const R=FP_COLLISION_RADIUS+2;if(!fpIsBlockedCircle(nx,fp.y,R))fp.x=nx;if(!fpIsBlockedCircle(fp.x,ny,R))fp.y=ny;if(fpIsBlockedCircle(fp.x,fp.y,R)){fp.avatar._fpVelF*=0.65;fp.avatar._fpVelR*=0.65;}}else{fp.x+=dxHeli;fp.y+=dyHeli;}
fp.x=Math.max(2,Math.min(fp.x,MAP_W*TILE-2));fp.y=Math.max(2,Math.min(fp.y,MAP_H*TILE-2));fp.avatar.x=fp.x;fp.avatar.y=fp.y;fp.avatar.alt=fp.avatar._fpAlt;fp.avatar.fpPitch=fp.avatar._fpPitch;fp.avatar.fpRoll=fp.avatar._fpRoll;fp.mouseYawDelta*=0.12;}else{let nx=fp.x+dx,ny=fp.y+dy;const R=FP_COLLISION_RADIUS;if(fp.avatar&&(fp.avatar.type==='crane'||fp.avatar.type==='train')){const railOk=(x,y)=>isRailTileForFaction(fp.avatar.faction,Math.floor(x/TILE),Math.floor(y/TILE));if(!fpIsBlockedCircle(nx,fp.y,R)&&railOk(nx,fp.y))fp.x=nx;else{const stepX=Math.sign(dx)*2;if(stepX&&!fpIsBlockedCircle(fp.x+stepX,fp.y,R)&&railOk(fp.x+stepX,fp.y))fp.x+=stepX;}
if(!fpIsBlockedCircle(fp.x,ny,R)&&railOk(fp.x,ny))fp.y=ny;else{const stepY=Math.sign(dy)*2;if(stepY&&!fpIsBlockedCircle(fp.x,fp.y+stepY,R)&&railOk(fp.x,fp.y+stepY))fp.y+=stepY;}}else{if(!fpIsBlockedCircle(nx,fp.y,R))fp.x=nx;else{const stepX=Math.sign(dx)*2;if(stepX&&!fpIsBlockedCircle(fp.x+stepX,fp.y,R))fp.x+=stepX;}
if(!fpIsBlockedCircle(fp.x,ny,R))fp.y=ny;else{const stepY=Math.sign(dy)*2;if(stepY&&!fpIsBlockedCircle(fp.x,fp.y+stepY,R))fp.y+=stepY;}}
fp.x=Math.max(2,Math.min(fp.x,MAP_W*TILE-2));fp.y=Math.max(2,Math.min(fp.y,MAP_H*TILE-2));fp.avatar.x=fp.x;fp.avatar.y=fp.y;}
try{const md=Math.hypot(fp.x-prevX,fp.y-prevY);if(fp.avatar&&fp.avatar.type==='leader'&&md>0.45)fp.avatar._fpMovingTick=tick;}catch(_){}
try{if(fp.avatar&&fp.avatar.type==='leader')leaderApplyRally(fp.avatar);}catch(_){}
fp.avatar.vx=0;fp.avatar.vy=0;fp.avatar.target=null;if(fp.avatar.type==='worker'){fp.avatar.state='idle';fp.avatar.mineTarget=null;fp.avatar.buildSite=null;fp.avatar.mineTimer=0;}
fp.fireCooldown=Math.max(0,fp.fireCooldown-1);const stats=fpGetWeaponStats(fp.avatar);if(!stats){fp.fireCharging=false;fp.fireCharge=0;fp.chargeShow=0;fp.fireReleased=false;return;}
const CHARGE_FRAMES=26;const CHARGE_SPEED=1/CHARGE_FRAMES;if(fp.fireHeld&&fp.fireCooldown===0){fp.fireCharging=true;fp.fireCharge=Math.min(1,fp.fireCharge+CHARGE_SPEED);}
const doAuto=(fp.fireHeld&&fp.fireCooldown===0&&fp.fireCharge>=1&&tick%6===0);if((fp.fireReleased||doAuto)&&fp.fireCooldown===0){const charge=Math.max(0.10,Math.min(1,fp.fireCharge));fpFireOnce(stats,charge);fp.fireCooldown=stats.fireRate;fp.fireCharge=0;fp.fireCharging=fp.fireHeld;fp.fireReleased=false;}
const coolProg=(fp.fireCooldown>0)?(1-fp.fireCooldown/Math.max(1,stats.fireRate)):0;const targetShow=fp.fireHeld?fp.fireCharge:(fp.fireCooldown>0?coolProg:0);fp.chargeShow=fp.chargeShow*0.72+targetShow*0.28;}
let player={id:'player',color:TEAM_COLORS.player.main,resources:80,bases:[],units:[],workers:[],trainQueue:[],tanks:[],patriots:[],supplies:[],launchers:[],towers:[],houses:[],apartments:[],trainings:[],villagers:[],drones:[],helicopters:[],walls:[],ships:[],harbors:[],rails:[],cranes:[],trains:[],silos:[],radars:[],leaders:[],bikes:[],discoveredEnemyStructures:new Set(),discoveredTiles:makeDiscoveryGrid(),};let ai={id:'ai',color:TEAM_COLORS.ai.main,resources:80,bases:[],units:[],workers:[],trainQueue:[],tanks:[],patriots:[],supplies:[],launchers:[],towers:[],houses:[],apartments:[],trainings:[],villagers:[],drones:[],helicopters:[],walls:[],ships:[],harbors:[],rails:[],cranes:[],trains:[],silos:[],radars:[],leaders:[],bikes:[],discoveredEnemyStructures:new Set(),discoveredTiles:makeDiscoveryGrid(),aiTick:0,waveTimer:0,waveSize:10,waveCount:0,buildPhase:true,buildPhaseEnd:4200,rallyPoints:[],scoutSent:false,};let selectedAIDifficulty='advanced';const AI_DIFFICULTIES={beginner:{id:'beginner',name:'Začátečník',armyMult:0.74,workerTrainInterval:340,baseWorkers:7,workerWaveScale:0.45,maxWorkers:11,workerDepositMult:1.04,workerHuntBias:14,workerHarassBias:20,siloTargetBias:120,smartnessBonus:14,maxBases:2,baseSchedule:[0,52000],baseBuildChance:0.22,expansionResourceBias:6.0,expansionDistanceBias:2.2,expansionCenterBias:1.7,expansionTries:12,rebuildMult:1.16,waveTimeout:2550,towerBonus:0,wallBonus:0,radarGoal:1,buildPhaseHarass:false,harassExtra:0,harassInterval:1200,forceThreeProngs:false,shockChance:0,nightmareResourceFight:false,maxLeaders:1,maxHelis:3,extraArmor:0.00,extraDrones:0},advanced:{id:'advanced',name:'Pokročilý',armyMult:1.0,workerTrainInterval:280,baseWorkers:10,workerWaveScale:0.6,maxWorkers:16,workerDepositMult:1.12,workerHuntBias:28,workerHarassBias:42,siloTargetBias:220,smartnessBonus:28,maxBases:4,baseSchedule:[0,21600,46800,75600],baseBuildChance:0.45,expansionResourceBias:8.5,expansionDistanceBias:2.8,expansionCenterBias:1.2,expansionTries:22,rebuildMult:1.0,waveTimeout:2100,towerBonus:0,wallBonus:0,radarGoal:1,buildPhaseHarass:false,harassExtra:0,harassInterval:960,forceThreeProngs:false,shockChance:0.05,nightmareResourceFight:false,maxLeaders:2,maxHelis:9,extraArmor:0.00,extraDrones:0},expert:{id:'expert',name:'Expert',armyMult:1.20,workerTrainInterval:240,baseWorkers:11,workerWaveScale:0.72,maxWorkers:18,workerDepositMult:1.26,workerHuntBias:44,workerHarassBias:68,siloTargetBias:340,smartnessBonus:54,maxBases:4,baseSchedule:[0,18000,37200,60000],baseBuildChance:0.62,expansionResourceBias:10.5,expansionDistanceBias:3.1,expansionCenterBias:1.0,expansionTries:34,rebuildMult:0.88,waveTimeout:1860,towerBonus:1,wallBonus:8,radarGoal:2,buildPhaseHarass:true,harassExtra:1,harassInterval:840,forceThreeProngs:true,shockChance:0.16,nightmareResourceFight:false,maxLeaders:2,maxHelis:7,extraArmor:0.03,extraDrones:1},nightmare:{id:'nightmare',name:'Noční můra',armyMult:1.58,workerTrainInterval:192,baseWorkers:12,workerWaveScale:0.88,maxWorkers:21,workerDepositMult:1.50,workerHuntBias:82,workerHarassBias:125,siloTargetBias:592,smartnessBonus:94,maxBases:5,baseSchedule:[0,12000,27000,43200,62400],baseBuildChance:0.82,expansionResourceBias:13.2,expansionDistanceBias:3.5,expansionCenterBias:0.78,expansionTries:52,rebuildMult:0.74,waveTimeout:1320,towerBonus:4,wallBonus:22,radarGoal:3,buildPhaseHarass:true,harassExtra:2,harassInterval:640,forceThreeProngs:true,shockChance:0.32,nightmareResourceFight:true,maxLeaders:3,maxHelis:6,extraArmor:0.07,extraDrones:4}};function getAIDifficultyCfg(id){return AI_DIFFICULTIES[id]||AI_DIFFICULTIES.advanced;}function currentAICfg(){return getAIDifficultyCfg((ai&&ai.difficultyId)||selectedAIDifficulty);}let map=[];let resourceHp={};function genMap(){map=[];resourceHp={};for(let y=0;y<MAP_H;y++){map[y]=[];for(let x=0;x<MAP_W;x++)map[y][x]=0;}
const numLakes=5+Math.floor(Math.random()*5);for(let li=0;li<numLakes;li++){let lx=20+Math.floor(Math.random()*(MAP_W-40));let ly=20+Math.floor(Math.random()*(MAP_H-40));let bigLake=Math.random()<0.16;let baseR=bigLake?(12+Math.floor(Math.random()*10)):(6+Math.floor(Math.random()*7));let numBlobs=bigLake?(3+Math.floor(Math.random()*3)):(2+Math.floor(Math.random()*2));for(let bi=0;bi<numBlobs;bi++){let bx=lx+Math.floor((Math.random()-0.5)*baseR*1.5);let by=ly+Math.floor((Math.random()-0.5)*baseR*1.5);let rx=baseR*(0.6+Math.random()*0.9);let ry=baseR*(0.6+Math.random()*0.9);for(let dy=-Math.ceil(ry)-1;dy<=Math.ceil(ry)+1;dy++){for(let dx=-Math.ceil(rx)-1;dx<=Math.ceil(rx)+1;dx++){let noise=(Math.random()-0.5)*0.35;if((dx*dx)/(rx*rx)+(dy*dy)/(ry*ry)<1+noise){let nx=bx+dx,ny=by+dy;if(nx>=1&&nx<MAP_W-1&&ny>=1&&ny<MAP_H-1)
map[ny][nx]=1;}}}}}
const numPonds=18+Math.floor(Math.random()*14);for(let pi=0;pi<numPonds;pi++){let px=8+Math.floor(Math.random()*(MAP_W-16));let py=8+Math.floor(Math.random()*(MAP_H-16));if((px<18&&py<18)||(px>MAP_W-19&&py>MAP_H-19))continue;let r=2+Math.floor(Math.random()*3);for(let dy=-r;dy<=r;dy++){for(let dx=-r;dx<=r;dx++){if(dx*dx+dy*dy>r*r+(Math.random()-0.5)*1.8)continue;let nx=px+dx,ny=py+dy;if(nx>=1&&nx<MAP_W-1&&ny>=1&&ny<MAP_H-1)map[ny][nx]=1;}}}
for(let pass=0;pass<2;pass++){let newMap=map.map(row=>[...row]);for(let y=1;y<MAP_H-1;y++){for(let x=1;x<MAP_W-1;x++){let waterCount=0;for(let dy=-1;dy<=1;dy++)for(let dx=-1;dx<=1;dx++)
if(map[y+dy][x+dx]===1)waterCount++;if(waterCount>=7)newMap[y][x]=1;else if(waterCount<=2)newMap[y][x]=0;}}
map=newMap;}
const clearR=12;for(let y=0;y<clearR;y++)for(let x=0;x<clearR;x++)map[y][x]=0;for(let y=MAP_H-clearR;y<MAP_H;y++)for(let x=MAP_W-clearR;x<MAP_W;x++)map[y][x]=0;let p1x=3,p1y=3,p2x=MAP_W-4,p2y=MAP_H-4;function bfsLand(sx,sy,ex,ey){let visited=Array.from({length:MAP_H},()=>new Array(MAP_W).fill(false));let queue=[{x:sx,y:sy}];visited[sy][sx]=true;while(queue.length){let{x,y}=queue.shift();if(x===ex&&y===ey)return true;for(let[dx,dy]of[[-1,0],[1,0],[0,-1],[0,1]]){let nx=x+dx,ny=y+dy;if(nx>=0&&nx<MAP_W&&ny>=0&&ny<MAP_H&&!visited[ny][nx]&&map[ny][nx]!==1){visited[ny][nx]=true;queue.push({x:nx,y:ny});}}}
return false;}
let northDone=false,southDone=false;if(!bfsLand(p1x,p1y,p2x,p2y)){let cx=p1x,cy=p1y;let targetY=Math.floor(MAP_H*0.12);let steps=0;while((Math.abs(cx-p2x)>2||Math.abs(cy-p2y)>2)&&steps<2000){let dx=p2x-cx,dy2=p2y-cy;if(cy>targetY&&Math.random()<0.7)cy--;else if(dx!==0)cx+=dx>0?1:-1;else cy+=dy2>0?1:-1;cx=Math.max(0,Math.min(cx,MAP_W-1));cy=Math.max(0,Math.min(cy,MAP_H-1));for(let oy=-1;oy<=1;oy++)for(let ox=-1;ox<=1;ox++){let nx=cx+ox,ny=cy+oy;if(nx>=0&&nx<MAP_W&&ny>=0&&ny<MAP_H)map[ny][nx]=0;}
steps++;}
northDone=true;}
{let cx=p1x,cy=p1y;let targetY=Math.floor(MAP_H*0.88);let steps=0;while((Math.abs(cx-p2x)>2||Math.abs(cy-p2y)>2)&&steps<2000){let dx=p2x-cx,dy2=p2y-cy;if(cy<targetY&&Math.random()<0.6)cy++;else if(dx!==0)cx+=dx>0?1:-1;else cy+=dy2>0?1:-1;cx=Math.max(0,Math.min(cx,MAP_W-1));cy=Math.max(0,Math.min(cy,MAP_H-1));for(let oy=-1;oy<=1;oy++)for(let ox=-1;ox<=1;ox++){let nx=cx+ox,ny=cy+oy;if(nx>=0&&nx<MAP_W&&ny>=0&&ny<MAP_H)map[ny][nx]=0;}
steps++;}}
if(!bfsLand(p1x,p1y,p2x,p2y)){let cx=p1x,cy=p1y,steps=0;while((Math.abs(cx-p2x)>2||Math.abs(cy-p2y)>2)&&steps<2000){let dx=p2x-cx,dy=p2y-cy;let r=Math.random();if(r<0.5)cx+=(dx>0?1:dx<0?-1:0);else cy+=(dy>0?1:dy<0?-1:0);cx=Math.max(0,Math.min(cx,MAP_W-1));cy=Math.max(0,Math.min(cy,MAP_H-1));for(let oy=-1;oy<=1;oy++)for(let ox=-1;ox<=1;ox++){let nx=cx+ox,ny=cy+oy;if(nx>=0&&nx<MAP_W&&ny>=0&&ny<MAP_H)map[ny][nx]=0;}
steps++;}}
for(let i=0;i<140;i++){let mx=5+Math.floor(Math.random()*(MAP_W-10));let my=5+Math.floor(Math.random()*(MAP_H-10));if(map[my][mx]===0){map[my][mx]=2;if(Math.random()<0.6&&my+1<MAP_H&&map[my+1][mx]===0)map[my+1][mx]=2;if(Math.random()<0.4&&mx+1<MAP_W&&map[my][mx+1]===0)map[my][mx+1]=2;}}
const addBoulder=(x,y)=>{if(x>=1&&x<MAP_W-1&&y>=1&&y<MAP_H-1&&map[y][x]===0)map[y][x]=8;};for(let y=2;y<MAP_H-2;y++){for(let x=2;x<MAP_W-2;x++){if(map[y][x]!==2)continue;if(Math.random()<0.28){const r=2+Math.floor(Math.random()*3);for(let dy=-r;dy<=r;dy++){for(let dx=-r;dx<=r;dx++){const d2=dx*dx+dy*dy;if(d2>r*r)continue;if(Math.random()<0.55)addBoulder(x+dx,y+dy);}}}}}
const rockFields=10+Math.floor(Math.random()*8);for(let i=0;i<rockFields;i++){const cx=10+Math.floor(Math.random()*(MAP_W-20));const cy=10+Math.floor(Math.random()*(MAP_H-20));if((cx<18&&cy<18)||(cx>MAP_W-19&&cy>MAP_H-19))continue;const r=3+Math.floor(Math.random()*4);for(let dy=-r;dy<=r;dy++){for(let dx=-r;dx<=r;dx++){if(dx*dx+dy*dy>r*r+(Math.random()-0.5)*2.2)continue;if(Math.random()<0.52)addBoulder(cx+dx,cy+dy);}}}
if(Math.random()<0.22){const tries=40;for(let tr=0;tr<tries;tr++){let sx=15+Math.floor(Math.random()*(MAP_W-30));let sy=15+Math.floor(Math.random()*(MAP_H-30));if((sx<22&&sy<22)||(sx>MAP_W-23&&sy>MAP_H-23))continue;if(map[sy][sx]!==0)continue;const horiz=Math.random()<0.5;const len=14+Math.floor(Math.random()*15);let ok=true;for(let i=0;i<len;i++){const x=sx+(horiz?i:0);const y=sy+(horiz?0:i);if(x<2||x>=MAP_W-2||y<2||y>=MAP_H-2){ok=false;break;}
const tt=map[y][x];if(tt===1||tt===3){ok=false;break;}
if(tt===2){ok=false;break;}}
if(!ok)continue;for(let i=0;i<len;i++){const x=sx+(horiz?i:0);const y=sy+(horiz?0:i);if(map[y][x]===0)map[y][x]=9;}
if(Math.random()<0.20){const bendAt=6+Math.floor(Math.random()*Math.max(1,len-12));const bx=sx+(horiz?bendAt:0);const by=sy+(horiz?0:bendAt);const len2=6+Math.floor(Math.random()*10);for(let i=1;i<=len2;i++){const x=bx+(horiz?0:i);const y=by+(horiz?i:0);if(x<2||x>=MAP_W-2||y<2||y>=MAP_H-2)break;if(map[y][x]===0)map[y][x]=9;}}
break;}}
const centerX=MAP_W/2,centerY=MAP_H/2;const totalResources=160;for(let i=0;i<totalResources;i++){let rx,ry;if(i<totalResources/2){let angle=Math.random()*Math.PI*2;let r1=Math.random()+Math.random();let radius=(r1/2)*Math.min(MAP_W,MAP_H)*0.22;rx=Math.round(centerX+Math.cos(angle)*radius);ry=Math.round(centerY+Math.sin(angle)*radius);rx=Math.max(5,Math.min(rx,MAP_W-6));ry=Math.max(5,Math.min(ry,MAP_H-6));}else{rx=5+Math.floor(Math.random()*(MAP_W-10));ry=5+Math.floor(Math.random()*(MAP_H-10));}
if(map[ry][rx]===0){map[ry][rx]=3;if(ry+1<MAP_H&&map[ry+1][rx]===0)map[ry+1][rx]=3;if(rx+1<MAP_W&&map[ry][rx+1]===0)map[ry][rx+1]=3;}}
for(let i=0;i<90;i++){let fx=6+Math.floor(Math.random()*(MAP_W-12));let fy=6+Math.floor(Math.random()*(MAP_H-12));if(map[fy][fx]!==0)continue;let size=8+Math.floor(Math.random()*23);let cx=fx,cy=fy;for(let s=0;s<size;s++){if(cx>=1&&cx<MAP_W-1&&cy>=1&&cy<MAP_H-1&&map[cy][cx]===0)map[cy][cx]=5;let d=Math.floor(Math.random()*4);cx+=[1,-1,0,0][d];cy+=[0,0,1,-1][d];cx=Math.max(1,Math.min(cx,MAP_W-2));cy=Math.max(1,Math.min(cy,MAP_H-2));}}
for(let y=1;y<MAP_H-1;y++){for(let x=1;x<MAP_W-1;x++){if(map[y][x]!==0)continue;let adjWater=false;for(let[dx,dy]of[[-1,0],[1,0],[0,-1],[0,1]]){if(map[y+dy]&&map[y+dy][x+dx]===1){adjWater=true;break;}}
if(adjWater&&Math.random()<0.12)map[y][x]=6;}}
for(let i=0;i<80;i++){let cx=5+Math.floor(Math.random()*(MAP_W-10));let cy=5+Math.floor(Math.random()*(MAP_H-10));if(map[cy][cx]===0)map[cy][cx]=7;}
for(let i=0;i<18;i++){let rx=5+Math.floor(Math.random()*(MAP_W-10));let ry=5+Math.floor(Math.random()*(MAP_H-10));if(map[ry][rx]!==1)continue;let boulderCount=2+Math.floor(Math.random()*2);let bx=rx,by=ry;for(let b=0;b<boulderCount;b++){if(bx>=1&&bx<MAP_W-1&&by>=1&&by<MAP_H-1&&map[by][bx]===1)map[by][bx]=10;let d=Math.floor(Math.random()*4);bx+=[1,-1,0,0][d];by+=[0,0,1,-1][d];}}
for(let i=0;i<34;i++){let cx=6+Math.floor(Math.random()*(MAP_W-12));let cy=6+Math.floor(Math.random()*(MAP_H-12));if(![0,5,7].includes(map[cy][cx]))continue;const size=6+Math.floor(Math.random()*18);let x=cx,y=cy;for(let s=0;s<size;s++){if(x>=1&&x<MAP_W-1&&y>=1&&y<MAP_H-1){if(map[y][x]===0||map[y][x]===5||map[y][x]===7)map[y][x]=8;}
const d=Math.floor(Math.random()*4);x+=[1,-1,0,0][d];y+=[0,0,1,-1][d];x=Math.max(1,Math.min(x,MAP_W-2));y=Math.max(1,Math.min(y,MAP_H-2));}}
const EXTRA_RICH_RESOURCE_VALUE=7800;
const EXTRA_RICH_RESOURCE_HITS=Math.max(1,Math.round(EXTRA_RICH_RESOURCE_VALUE/WORKER_CARRY));
function placeExtraRichResourceCluster(){
const baseA={x:4,y:4},baseB={x:MAP_W-4,y:MAP_H-4};
const midX=(baseA.x+baseB.x)/2,midY=(baseA.y+baseB.y)/2;
const segX=baseB.x-baseA.x,segY=baseB.y-baseA.y;
let perpX=-segY,perpY=segX;
const perpLen=Math.hypot(perpX,perpY)||1;
perpX/=perpLen;perpY/=perpLen;
const offset=24+Math.random()*16;
const sign=Math.random()<0.5?-1:1;
const anchorX=Math.round(midX+perpX*offset*sign);
const anchorY=Math.round(midY+perpY*offset*sign);
let best=null,bestScore=-1e18;
for(let oy=-10;oy<=10;oy++){
for(let ox=-10;ox<=10;ox++){
const tx=Math.max(8,Math.min(MAP_W-9,anchorX+ox));
const ty=Math.max(8,Math.min(MAP_H-9,anchorY+oy));
const centerDist=Math.hypot(tx-midX,ty-midY);
if(centerDist<Math.min(MAP_W,MAP_H)*0.15)continue;
const balance=Math.abs(Math.hypot(tx-baseA.x,ty-baseA.y)-Math.hypot(tx-baseB.x,ty-baseB.y));
if(balance>12)continue;
let open=0,water=0;
for(let yy=-5;yy<=5;yy++){
for(let xx=-5;xx<=5;xx++){
if(xx*xx+yy*yy>34)continue;
const nx=tx+xx,ny=ty+yy;
if(nx<1||nx>=MAP_W-1||ny<1||ny>=MAP_H-1){water++;continue;}
if(map[ny][nx]===1)water++; else open++;
}
}
if(open<56)continue;
const score=open*2-water*6-balance*4-centerDist*0.05;
if(score>bestScore){bestScore=score;best={tx,ty};}
}
}
if(!best)best={tx:Math.max(8,Math.min(MAP_W-9,anchorX)),ty:Math.max(8,Math.min(MAP_H-9,anchorY))};
const cells=[];
for(let oy=-8;oy<=8;oy++){
for(let ox=-8;ox<=8;ox++){
const d2=ox*ox+oy*oy;
if(d2>42)continue;
cells.push({ox,oy,score:d2+Math.abs(ox)*0.03+Math.abs(oy)*0.03+Math.random()*0.01});
}
}
cells.sort((a,b)=>a.score-b.score);
let remaining=EXTRA_RICH_RESOURCE_HITS;
for(const c of cells){
if(remaining<=0)break;
const tx=best.tx+c.ox,ty=best.ty+c.oy;
if(tx<2||tx>=MAP_W-2||ty<2||ty>=MAP_H-2)continue;
if(map[ty][tx]===1||map[ty][tx]===3||map[ty][tx]===9||map[ty][tx]===10)continue;
map[ty][tx]=3;
const hp=Math.min(RESOURCE_HP,remaining);
resourceHp[`${tx},${ty}`]=hp;
remaining-=hp;
}
if(remaining>0){
for(let r=9;r<=14&&remaining>0;r++){
for(let oy=-r;oy<=r&&remaining>0;oy++){
for(let ox=-r;ox<=r&&remaining>0;ox++){
if(Math.abs(ox)!==r&&Math.abs(oy)!==r)continue;
const tx=best.tx+ox,ty=best.ty+oy;
if(tx<2||tx>=MAP_W-2||ty<2||ty>=MAP_H-2)continue;
if(map[ty][tx]===1||map[ty][tx]===3||map[ty][tx]===9||map[ty][tx]===10)continue;
map[ty][tx]=3;
const hp=Math.min(RESOURCE_HP,remaining);
resourceHp[`${tx},${ty}`]=hp;
remaining-=hp;
}
}
}
}
for(let oy=-1;oy<=1;oy++){
for(let ox=-1;ox<=1;ox++){
const tx=best.tx+ox,ty=best.ty+oy;
if(tx<1||tx>=MAP_W-1||ty<1||ty>=MAP_H-1)continue;
if(map[ty][tx]!==1&&map[ty][tx]!==3)map[ty][tx]=0;
}
}
}
placeExtraRichResourceCluster();
for(let y=0;y<MAP_H;y++)
for(let x=0;x<MAP_W;x++)
if(map[y][x]===3&&!resourceHp[`${x},${y}`])resourceHp[`${x},${y}`]=RESOURCE_HP;}
function depleteResource(tx,ty){let key=`${tx},${ty}`;if(!resourceHp[key])return;resourceHp[key]--;if(MP.active)MP.sendCmd({action:'mine',tx,ty});if(resourceHp[key]<=0){map[ty][tx]=0;delete resourceHp[key];invalidateTerrainCaches();resourceTileCache=resourceTileCache.filter(r=>!(r.tx===tx&&r.ty===ty));log(`Zdroj na [${tx},${ty}] vyčerpán!`,'combat-log');}}
let _nextUnitId=1;function nextUnitId(){return _nextUnitId++;}
function mpNextUnitId(){const n=nextUnitId();if(MP&&MP.faction){const pref=(MP.faction==='player')?'p1':'p2';return`${pref}-${n}`;}
return n;}
function makeBase(faction,x,y){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'base',faction,x,y,hp:BASE_HP,maxHp:BASE_HP,w:2,h:2,id:`base_${tx}_${ty}`};}
function makeUnit(faction,x,y){return{type:'unit',faction,x,y,hp:UNIT_HP,maxHp:UNIT_HP,vx:0,vy:0,target:null,attackTarget:null,pendingAttack:null,fireCooldown:0,mineCooldown:0,id:Math.random()};}
function makeWorker(faction,x,y){return{type:'worker',faction,x,y,hp:WORKER_HP,maxHp:WORKER_HP,vx:0,vy:0,target:null,attackTarget:null,fireCooldown:0,id:Math.random(),carry:0,mineTimer:0,state:'idle',mineTarget:null,homeBase:null,dropoffTarget:null,buildSite:null,repairTarget:null};}
function makeVillager(faction,x,y){return{type:'villager',faction,x,y,hp:VILLAGER_HP,maxHp:VILLAGER_HP,vx:0,vy:0,target:null,fleeTimer:0,life:VILLAGER_LIFE,trainTarget:null,trainTimer:0,hidden:false,id:Math.random()};}
function makeTank(faction,x,y){return{type:'tank',faction,x,y,hp:TANK_HP_NEW,maxHp:TANK_HP_NEW,vx:0,vy:0,target:null,attackTarget:null,pendingAttack:null,fireCooldown:0,id:Math.random()};}
let particles=[];function makeHouse(faction,x,y){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'house',faction,x,y,hp:HOUSE_HP,maxHp:HOUSE_HP,id:`house_${tx}_${ty}`};}
function makeApartment(faction,x,y){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'apartment',faction,x,y,hp:APARTMENT_HP,maxHp:APARTMENT_HP,spawnTimer:0,id:`apartment_${tx}_${ty}`};}
function makeTraining(faction,x,y){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'training',faction,x,y,hp:TRAINING_HP,maxHp:TRAINING_HP,cap:TRAINING_CAP,slots:[],id:`training_${tx}_${ty}`};}
function makeWall(faction,x,y){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'wall',faction,x,y,hp:WALL_HP,maxHp:WALL_HP,id:`wall_${tx}_${ty}`};}
function makeRail(faction,x,y){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'rail',faction,x,y,hp:RAIL_HP,maxHp:RAIL_HP,id:`rail_${tx}_${ty}`};}
function makeCrane(faction,x,y){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'crane',faction,x,y,hp:CRANE_HP,maxHp:CRANE_HP,id:`crane_${tx}_${ty}`,target:null,vx:0,vy:0};}
function makeTrain(faction,x,y){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'train',faction,x,y,hp:TRAIN_HP,maxHp:TRAIN_HP,id:`train_${tx}_${ty}`,target:null,vx:0,vy:0,stationName:'Nádraží',cargoWaiting:0,cargoDelivered:0,routeStatus:'offline',shuttle:null};}
function makeDrone(faction,x,y){return{type:'drone',faction,x,y,hp:DRONE_HP,maxHp:DRONE_HP,vx:0,vy:0,target:null,attackTarget:null,pendingAttack:null,fireCooldown:0,id:Math.random()};}
function makeHelicopter(faction,x,y){return{type:'helicopter',faction,x,y,hp:HELICOPTER_HP,maxHp:HELICOPTER_HP,vx:0,vy:0,target:null,attackTarget:null,pendingAttack:null,fireCooldown:0,bombCooldown:0,id:Math.random(),alt:HELICOPTER_ALTITUDE,rotorPhase:Math.random()*Math.PI*2,fpPitch:0,fpRoll:0,homeBase:null};}
function makeLeader(faction,x,y){return{type:'leader',faction,x,y,hp:LEADER_HP,maxHp:LEADER_HP,vx:0,vy:0,target:null,attackTarget:null,fireCooldown:0,id:Math.random(),rallyOffset:null};}
function makeBike(faction,x,y){return{type:'bike',faction,x,y,hp:BIKE_HP,maxHp:BIKE_HP,vx:0,vy:0,target:null,attackTarget:null,pendingAttack:null,fireCooldown:0,id:Math.random()};}
function makePatriot(faction,x,y){return{type:'patriot',faction,x,y,hp:PATRIOT_HP,maxHp:PATRIOT_HP,vx:0,vy:0,target:null,attackTarget:null,pendingAttack:null,fireCooldown:0,nukeCooldown:0,id:Math.random()};}
function makeSupply(faction,x,y){return{type:'supply',faction,x,y,hp:SUPPLY_HP,maxHp:SUPPLY_HP,vx:0,vy:0,target:null,attackTarget:null,pendingAttack:null,fireCooldown:0,id:Math.random()};}
function makeLauncher(faction,x,y){return{type:'launcher',faction,x,y,hp:LAUNCHER_HP,maxHp:LAUNCHER_HP,vx:0,vy:0,target:null,attackTarget:null,pendingAttack:null,fireCooldown:0,id:Math.random()};}
function makeMine(faction,x,y,placerId){return{type:'mine',faction,x,y,placerId:placerId||null,hp:1,maxHp:1,radius:MINE_KILL_RADIUS,damage:MINE_DAMAGE,armTimer:MINE_ARM_TICKS,id:`mine_${Math.random()}`};}
function popCap(faction){let fac=getFaction(faction);return fac.bases.length*BASE_POP_CAP+fac.houses.length*HOUSE_CAP+(fac.apartments?fac.apartments.length:0)*APARTMENT_CAP;}
function popUsed(faction){let fac=getFaction(faction);const v=fac.villagers?fac.villagers.length:0;const heli=fac.helicopters?fac.helicopters.length*HELICOPTER_POP_COST:0;return fac.units.length+fac.workers.length+fac.tanks.length+(fac.patriots?fac.patriots.length*PATRIOT_POP_COST:0)+(fac.supplies?fac.supplies.length*SUPPLY_POP_COST:0)+(fac.launchers?fac.launchers.length*LAUNCHER_POP_COST:0)+fac.leaders.length+v+(fac.bikes?fac.bikes.length:0)+heli;}
function makeTower(faction,x,y,isSuper=false){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'tower',faction,x,y,hp:isSuper?SUPER_TOWER_HP:TOWER_HP,maxHp:isSuper?SUPER_TOWER_HP:TOWER_HP,fireCooldown:0,nearCooldown:0,farCooldown:0,isSuper:!!isSuper,id:`tower_${isSuper?'super_':''}${tx}_${ty}`};}
function makeHarbor(faction,x,y){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'harbor',faction,x,y,hp:HARBOR_HP,maxHp:HARBOR_HP,id:`harbor_${tx}_${ty}`};}
function makeSilo(faction,x,y){const tx=Math.round(x/TILE),ty=Math.round(y/TILE);return{type:'silo',faction,x,y,hp:SILO_HP,maxHp:SILO_HP,id:`silo_${tx}_${ty}`,nukeTarget:null,nukeTimer:0,nukeMaxTimer:NUKE_COUNTDOWN,nukeUsed:false,nukeDetonated:false,nukeIntercepted:false};}
function makeRadar(faction,x,y){const tx=Math.round(x/32),ty=Math.round(y/32);return{type:'radar',faction,x,y,hp:RADAR_HP,maxHp:RADAR_HP,id:`radar_${tx}_${ty}`};}
function makeShip(faction,x,y){return{type:'ship',faction,x,y,hp:SHIP_HP,maxHp:SHIP_HP,vx:0,vy:0,target:null,attackTarget:null,pendingAttack:null,fireCooldown:0,id:Math.random()};}
const SUPPLY_AURA_TYPES=new Set(['unit','worker','villager','tank','drone','helicopter','patriot','supply','bike','leader','ship','launcher']);
function isSupplyAuraBuffable(ent){return !!(ent&&SUPPLY_AURA_TYPES.has(ent.type));}
function isEntityInSupplyAura(ent){if(!ent||ent.hp<=0||!isSupplyAuraBuffable(ent))return false;const fac=getFaction(ent.faction);const supplies=(fac&&fac.supplies)||[];const c=entityCenter(ent);const rr=SUPPLY_AURA_RADIUS*SUPPLY_AURA_RADIUS;for(let i=0;i<supplies.length;i++){const s=supplies[i];if(!s||s===ent||s.hp<=0)continue;const sc=entityCenter(s);const dx=c.x-sc.x,dy=c.y-sc.y;if(dx*dx+dy*dy<=rr)return true;}return false;}
function pointSegDistSq(px,py,ax,ay,bx,by){const abx=bx-ax,aby=by-ay;const apx=px-ax,apy=py-ay;const ab2=abx*abx+aby*aby;if(ab2<=0.0001){const dx=px-ax,dy=py-ay;return dx*dx+dy*dy;}let t=(apx*abx+apy*aby)/ab2;t=Math.max(0,Math.min(1,t));const cx=ax+abx*t,cy=ay+aby*t;const dx=px-cx,dy=py-cy;return dx*dx+dy*dy;}

